diff --git a/components/filedialog/FolderContents.qml b/components/filedialog/FolderContents.qml
index 6238535dd758a76a6bcf798bc8b0ccf7eb62f1e1..7279416d8f1c098df0f4ea67b52f71625d463a5e 100644
--- a/components/filedialog/FolderContents.qml
+++ b/components/filedialog/FolderContents.qml
@@ -60,7 +60,7 @@ Item {
             }
 
             StyledText {
-                text: qsTr("This folder is empty")
+                text: qsTr("此文件夹为空")
                 color: Colours.palette.m3outline
                 font: Tokens.font.body.builders.large.weight(Font.Medium).build()
             }
diff --git a/components/filedialog/Sidebar.qml b/components/filedialog/Sidebar.qml
index a8d2cf15dd84c4c76264749056906e6f511807da..f0d5e6722da9f3524d36c6195fcd4288e7b59e07 100644
--- a/components/filedialog/Sidebar.qml
+++ b/components/filedialog/Sidebar.qml
@@ -30,7 +30,7 @@ StyledRect {
             Layout.alignment: Qt.AlignHCenter
             Layout.topMargin: Tokens.padding.extraSmall / 2
             Layout.bottomMargin: Tokens.spacing.medium
-            text: qsTr("Files")
+            text: qsTr("文件")
             color: Colours.palette.m3onSurface
             font: Tokens.font.body.builders.large.weight(Font.Bold).build()
         }
diff --git a/modules/BatteryMonitor.qml b/modules/BatteryMonitor.qml
index 48da6d007110c64b9f2f9d928acb020ca55daaa5..62d8ff33f2dcba29fcc058992a7f0ce01463824b 100644
--- a/modules/BatteryMonitor.qml
+++ b/modules/BatteryMonitor.qml
@@ -14,10 +14,10 @@ Scope {
         function onOnBatteryChanged(): void {
             if (UPower.onBattery) {
                 if (GlobalConfig.utilities.toasts.chargingChanged)
-                    Toaster.toast(qsTr("Charger unplugged"), qsTr("Battery is discharging"), "power_off");
+                    Toaster.toast(qsTr("充电器已拔出"), qsTr("电池正在放电"), "power_off");
             } else {
                 if (GlobalConfig.utilities.toasts.chargingChanged)
-                    Toaster.toast(qsTr("Charger plugged in"), qsTr("Battery is charging"), "power");
+                    Toaster.toast(qsTr("充电器已连接"), qsTr("电池正在充电"), "power");
                 for (const level of root.warnLevels)
                     level.warned = false;
             }
@@ -35,12 +35,12 @@ Scope {
             for (const level of root.warnLevels) {
                 if (p <= level.level && !level.warned) {
                     level.warned = true;
-                    Toaster.toast(level.title ?? qsTr("Battery warning"), level.message ?? qsTr("Battery level is low"), level.icon ?? "battery_android_alert", level.critical ? Toast.Error : Toast.Warning);
+                    Toaster.toast(level.title ?? qsTr("电量警告"), level.message ?? qsTr("电池电量较低"), level.icon ?? "battery_android_alert", level.critical ? Toast.Error : Toast.Warning);
                 }
             }
 
             if (!hibernateTimer.running && p <= GlobalConfig.general.battery.criticalLevel) {
-                Toaster.toast(qsTr("Hibernating in 5 seconds"), qsTr("Hibernating to prevent data loss"), "battery_android_alert", Toast.Error);
+                Toaster.toast(qsTr("将在 5 秒后休眠"), qsTr("正在休眠以防止数据丢失"), "battery_android_alert", Toast.Error);
                 hibernateTimer.start();
             }
         }
diff --git a/modules/ConfigToasts.qml b/modules/ConfigToasts.qml
index 81463b8d838bcde51b51a5804424098f8403cfcb..ce65eff5ee40ed4ce79f20ed533abf2360ed33e6 100644
--- a/modules/ConfigToasts.qml
+++ b/modules/ConfigToasts.qml
@@ -7,19 +7,19 @@ Scope {
     Connections {
         function onLoaded(): void {
             if (GlobalConfig.utilities.toasts.configLoaded)
-                Toaster.toast(qsTr("Config loaded"), qsTr("Config loaded successfully!"), "rule_settings");
+                Toaster.toast(qsTr("配置已加载"), qsTr("配置已成功加载"), "rule_settings");
         }
 
         function onLoadFailed(error: string, screen: string): void {
-            Toaster.toast(qsTr("Failed to parse config%1").arg(screen ? " for " + screen : ""), error, "settings_alert", Toast.Warning);
+            Toaster.toast(qsTr("无法解析%1配置").arg(screen ? " for " + screen : ""), error, "settings_alert", Toast.Warning);
         }
 
         function onSaveFailed(error: string, screen: string): void {
-            Toaster.toast(qsTr("Failed to save config%1").arg(screen ? " for " + screen : ""), error, "settings_alert", Toast.Error);
+            Toaster.toast(qsTr("无法保存%1配置").arg(screen ? " for " + screen : ""), error, "settings_alert", Toast.Error);
         }
 
         function onUnknownOption(key: string, screen: string): void {
-            Toaster.toast(qsTr("Unknown option in%1 config").arg(screen ? " " + screen : ""), key, "question_mark", Toast.Warning);
+            Toaster.toast(qsTr("%1配置中存在未知选项").arg(screen ? " " + screen : ""), key, "question_mark", Toast.Warning);
         }
 
         target: GlobalConfig
@@ -27,11 +27,11 @@ Scope {
 
     Connections {
         function onLoadFailed(error: string, screen: string): void {
-            Toaster.toast(qsTr("Failed to parse token config%1").arg(screen ? "for " + screen : ""), error, "settings_alert", Toast.Warning);
+            Toaster.toast(qsTr("无法解析%1令牌配置").arg(screen ? "for " + screen : ""), error, "settings_alert", Toast.Warning);
         }
 
         function onUnknownOption(key: string, screen: string): void {
-            Toaster.toast(qsTr("Unknown option in%1 token config").arg(screen ? " " + screen : ""), key, "question_mark", Toast.Warning);
+            Toaster.toast(qsTr("%1令牌配置中存在未知选项").arg(screen ? " " + screen : ""), key, "question_mark", Toast.Warning);
         }
 
         target: TokenConfig
diff --git a/modules/Shortcuts.qml b/modules/Shortcuts.qml
index 68ffe6501913ee9353d1776eb4cce92a197b547e..b9c333705581c8d2cbed70854b50fae19483798b 100644
--- a/modules/Shortcuts.qml
+++ b/modules/Shortcuts.qml
@@ -16,7 +16,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "nexus"
-        description: "Open nexus"
+        description: "打开设置中心"
         onPressed: WindowFactory.create()
     }
 
@@ -24,7 +24,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "showall"
-        description: "Toggle launcher, dashboard and osd"
+        description: "切换启动器、仪表盘和屏幕提示"
         onPressed: {
             if (root.hasFullscreen)
                 return;
@@ -37,7 +37,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "dashboard"
-        description: "Toggle dashboard"
+        description: "切换仪表盘"
         onPressed: {
             if (root.hasFullscreen)
                 return;
@@ -50,7 +50,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "session"
-        description: "Toggle session menu"
+        description: "切换会话菜单"
         onPressed: {
             if (root.hasFullscreen)
                 return;
@@ -63,7 +63,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "launcher"
-        description: "Toggle launcher"
+        description: "切换启动器"
         onPressed: root.launcherInterrupted = false
         onReleased: {
             if (!root.launcherInterrupted && !root.hasFullscreen) {
@@ -78,7 +78,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "launcherInterrupt"
-        description: "Interrupt launcher keybind"
+        description: "中断启动器快捷键"
         onPressed: root.launcherInterrupted = true
     }
 
@@ -86,7 +86,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "sidebar"
-        description: "Toggle sidebar"
+        description: "切换侧边栏"
         onPressed: {
             if (root.hasFullscreen)
                 return;
@@ -99,7 +99,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "utilities"
-        description: "Toggle utilities"
+        description: "切换实用工具"
         onPressed: {
             if (root.hasFullscreen)
                 return;
diff --git a/modules/areapicker/AreaPicker.qml b/modules/areapicker/AreaPicker.qml
index 76cc10399c3f75545b3d265a8ae9088489b2a872..09d95b8d6717da02ba3f7f98f220a4df95259355 100644
--- a/modules/areapicker/AreaPicker.qml
+++ b/modules/areapicker/AreaPicker.qml
@@ -83,7 +83,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "screenshot"
-        description: "Open screenshot tool"
+        description: "打开截图工具"
         onPressed: {
             root.freeze = false;
             root.closing = false;
@@ -96,7 +96,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "screenshotFreeze"
-        description: "Open screenshot tool (freeze mode)"
+        description: "打开截图工具（冻结画面）"
         onPressed: {
             root.freeze = true;
             root.closing = false;
@@ -109,7 +109,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "screenshotClip"
-        description: "Open screenshot tool (clipboard)"
+        description: "打开截图工具（复制到剪贴板）"
         onPressed: {
             root.freeze = false;
             root.closing = false;
@@ -122,7 +122,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "screenshotFreezeClip"
-        description: "Open screenshot tool (freeze mode, clipboard)"
+        description: "打开截图工具（冻结画面并复制到剪贴板）"
         onPressed: {
             root.freeze = true;
             root.closing = false;
diff --git a/modules/background/Wallpaper.qml b/modules/background/Wallpaper.qml
index 21ff34eb8f9893f4eaeb6530cf46b71e0ca64878..1d5dde51e242450988a40dded9146bf79bbad274 100644
--- a/modules/background/Wallpaper.qml
+++ b/modules/background/Wallpaper.qml
@@ -58,7 +58,7 @@ Item {
                     spacing: Tokens.spacing.small
 
                     StyledText {
-                        text: qsTr("Wallpaper missing?")
+                        text: qsTr("壁纸丢失了吗？")
                         color: Colours.palette.m3onSurfaceVariant
                         font: Tokens.font.body.builders.large.size(28 * 2).weight(Font.Bold).build()
                     }
@@ -73,8 +73,8 @@ Item {
                         FileDialog {
                             id: dialog
 
-                            title: qsTr("Select a wallpaper")
-                            filterLabel: qsTr("Image files")
+                            title: qsTr("选择壁纸")
+                            filterLabel: qsTr("图片文件")
                             filters: Images.validImageExtensions
                             onAccepted: path => Wallpapers.setWallpaper(path)
                         }
@@ -90,7 +90,7 @@ Item {
 
                             anchors.centerIn: parent
 
-                            text: qsTr("Set it now!")
+                            text: qsTr("立即设置")
                             color: Colours.palette.m3onPrimary
                             font: Tokens.font.body.large
                         }
diff --git a/modules/bar/components/ActiveWindow.qml b/modules/bar/components/ActiveWindow.qml
index 8e34e8b136c6cee47d48de37e8f3de1a39f9b7cb..a42675edcc81f50fd4eeca7905ebfef522621b8a 100644
--- a/modules/bar/components/ActiveWindow.qml
+++ b/modules/bar/components/ActiveWindow.qml
@@ -16,7 +16,7 @@ Item {
     readonly property string windowTitle: {
         const title = Hypr.activeToplevel?.title;
         if (!title)
-            return qsTr("Desktop");
+            return qsTr("桌面");
         if (Config.bar.activeWindow.compact) {
             // " - " (standard hyphen), " — " (em dash), " – " (en dash)
             const parts = title.split(/\s+[\-\u2013\u2014]\s+/);
diff --git a/modules/bar/popouts/Audio.qml b/modules/bar/popouts/Audio.qml
index 546fedbd2251c4fad7dda71c2b2ce9d83a9cc319..1ed8be3c91ebfd880ff94203d61b20ed2238fde6 100644
--- a/modules/bar/popouts/Audio.qml
+++ b/modules/bar/popouts/Audio.qml
@@ -34,7 +34,7 @@ Item {
         spacing: Tokens.spacing.medium
 
         StyledText {
-            text: qsTr("Output device")
+            text: qsTr("输出设备")
             font: Tokens.font.body.builders.medium.weight(Font.Medium).build()
         }
 
@@ -55,7 +55,7 @@ Item {
 
         StyledText {
             Layout.topMargin: Tokens.spacing.medium
-            text: qsTr("Input device")
+            text: qsTr("输入设备")
             font: Tokens.font.body.builders.medium.weight(Font.Medium).build()
         }
 
@@ -74,7 +74,7 @@ Item {
 
         StyledText {
             Layout.topMargin: Tokens.spacing.medium
-            text: qsTr("Volume (%1)").arg(Audio.muted ? qsTr("Muted") : `${Math.round(Audio.volume * 100)}%`)
+            text: qsTr("音量（%1）").arg(Audio.muted ? qsTr("已静音") : `${Math.round(Audio.volume * 100)}%`)
             font: Tokens.font.body.builders.medium.weight(Font.Medium).build()
         }
 
diff --git a/modules/bar/popouts/Battery.qml b/modules/bar/popouts/Battery.qml
index 3c3ced517ace1294983caed0e5faf9a2da627b48..e27c5621a01c2159c66be96a8ef15e523b3e1768 100644
--- a/modules/bar/popouts/Battery.qml
+++ b/modules/bar/popouts/Battery.qml
@@ -13,7 +13,7 @@ Column {
     width: Tokens.sizes.bar.batteryWidth
 
     StyledText {
-        text: UPower.displayDevice.isLaptopBattery ? qsTr("Remaining: %1%").arg(Math.round(UPower.displayDevice.percentage * 100)) : qsTr("No battery detected")
+        text: UPower.displayDevice.isLaptopBattery ? qsTr("剩余：%1%").arg(Math.round(UPower.displayDevice.percentage * 100)) : qsTr("未检测到电池")
     }
 
     StyledText {
@@ -33,7 +33,7 @@ Column {
             return comps.join(", ") || fallback;
         }
 
-        text: UPower.displayDevice.isLaptopBattery ? qsTr("Time %1: %2").arg(UPower.onBattery ? "remaining" : "until charged").arg(UPower.onBattery ? formatSeconds(UPower.displayDevice.timeToEmpty, "Calculating...") : formatSeconds(UPower.displayDevice.timeToFull, "Fully charged!")) : qsTr("Power profile: %1").arg(PowerProfile.toString(PowerProfiles.profile))
+        text: UPower.displayDevice.isLaptopBattery ? qsTr("%1：%2").arg(UPower.onBattery ? "剩余时间" : "充满时间").arg(UPower.onBattery ? formatSeconds(UPower.displayDevice.timeToEmpty, "正在计算…") : formatSeconds(UPower.displayDevice.timeToFull, "已充满！")) : qsTr("电源模式：%1").arg(PowerProfile.toString(PowerProfiles.profile))
     }
 
     Loader {
@@ -70,7 +70,7 @@ Column {
 
                     StyledText {
                         anchors.verticalCenter: parent.verticalCenter
-                        text: qsTr("Performance Degraded")
+                        text: qsTr("性能受限")
                         color: Colours.palette.m3onError
                         font: Tokens.font.mono.builders.medium.weight(Font.Medium).build()
                     }
@@ -87,7 +87,7 @@ Column {
                 StyledText {
                     anchors.horizontalCenter: parent.horizontalCenter
 
-                    text: qsTr("Reason: %1").arg(PerformanceDegradationReason.toString(PowerProfiles.degradationReason))
+                    text: qsTr("原因：%1").arg(PerformanceDegradationReason.toString(PowerProfiles.degradationReason))
                     color: Colours.palette.m3onError
                 }
             }
diff --git a/modules/bar/popouts/Bluetooth.qml b/modules/bar/popouts/Bluetooth.qml
index ebf21de30ee83f232e273d8cb29de34e67e7d3ab..dfa9bf0213bbf47d3f3b2c10a994f52a5870d3fe 100644
--- a/modules/bar/popouts/Bluetooth.qml
+++ b/modules/bar/popouts/Bluetooth.qml
@@ -21,12 +21,12 @@ ColumnLayout {
     StyledText {
         Layout.topMargin: Tokens.padding.medium
         Layout.rightMargin: Tokens.padding.extraSmall
-        text: qsTr("Bluetooth")
+        text: qsTr("蓝牙")
         font: Tokens.font.body.builders.medium.weight(Font.Medium).build()
     }
 
     Toggle {
-        label: qsTr("Enabled")
+        label: qsTr("启用")
         checked: Bluetooth.defaultAdapter?.enabled ?? false // qmllint disable unresolved-type
         toggle.onToggled: {
             const adapter = Bluetooth.defaultAdapter; // qmllint disable unresolved-type
@@ -36,7 +36,7 @@ ColumnLayout {
     }
 
     Toggle {
-        label: qsTr("Discovering")
+        label: qsTr("正在发现")
         checked: Bluetooth.defaultAdapter?.discovering ?? false // qmllint disable unresolved-type
         toggle.onToggled: {
             const adapter = Bluetooth.defaultAdapter; // qmllint disable unresolved-type
@@ -177,7 +177,7 @@ ColumnLayout {
         inactiveColour: Colours.palette.m3primaryContainer
         inactiveOnColour: Colours.palette.m3onPrimaryContainer
         verticalPadding: Tokens.padding.extraSmall
-        text: qsTr("Open settings")
+        text: qsTr("打开设置")
         icon: "settings"
 
         onClicked: root.popouts.detachRequested("bluetooth")
diff --git a/modules/bar/popouts/LockStatus.qml b/modules/bar/popouts/LockStatus.qml
index caab7487f41c19bdfa59d9b471b7a01dcbe8e8f6..8fccd425474d38cffd52bd5a5baffd5639e8edd1 100644
--- a/modules/bar/popouts/LockStatus.qml
+++ b/modules/bar/popouts/LockStatus.qml
@@ -7,10 +7,10 @@ ColumnLayout {
     spacing: Tokens.spacing.small
 
     StyledText {
-        text: qsTr("Capslock: %1").arg(Hypr.capsLock ? "Enabled" : "Disabled")
+        text: qsTr("大写锁定：%1").arg(Hypr.capsLock ? "Enabled" : "Disabled")
     }
 
     StyledText {
-        text: qsTr("Numlock: %1").arg(Hypr.numLock ? "Enabled" : "Disabled")
+        text: qsTr("数字锁定：%1").arg(Hypr.numLock ? "Enabled" : "Disabled")
     }
 }
diff --git a/modules/bar/popouts/Network.qml b/modules/bar/popouts/Network.qml
index 4401330b66a37ff45134980d8a303ee839ed86ce..d65ea4c4dd623136b1331505fbacc7cf5f50fcf5 100644
--- a/modules/bar/popouts/Network.qml
+++ b/modules/bar/popouts/Network.qml
@@ -28,14 +28,14 @@ ColumnLayout {
         Layout.preferredHeight: visible ? implicitHeight : 0
         Layout.topMargin: visible ? Tokens.padding.medium : 0
         Layout.rightMargin: Tokens.padding.extraSmall
-        text: qsTr("Wireless")
+        text: qsTr("无线网络")
         font: Tokens.font.body.builders.medium.weight(Font.Medium).build()
     }
 
     Toggle {
         visible: root.view === "wireless"
         Layout.preferredHeight: visible ? implicitHeight : 0
-        label: qsTr("Enabled")
+        label: qsTr("启用")
         checked: Nmcli.wifiEnabled
         toggle.onToggled: Nmcli.enableWifi(checked)
     }
@@ -45,7 +45,7 @@ ColumnLayout {
         Layout.preferredHeight: visible ? implicitHeight : 0
         Layout.topMargin: visible ? Tokens.spacing.small : 0
         Layout.rightMargin: Tokens.padding.extraSmall
-        text: qsTr("%1 networks available").arg(Nmcli.networks.length) // qmllint disable missing-property
+        text: qsTr("可用网络：%1 个").arg(Nmcli.networks.length) // qmllint disable missing-property
         color: Colours.palette.m3onSurfaceVariant
         font: Tokens.font.body.small
     }
@@ -200,7 +200,7 @@ ColumnLayout {
 
             StyledText {
                 Layout.topMargin: -Math.round(scanIcon.fontInfo.pointSize * 0.0575)
-                text: qsTr("Rescan networks")
+                text: qsTr("重新扫描网络")
                 color: Colours.palette.m3onPrimaryContainer
             }
 
@@ -226,7 +226,7 @@ ColumnLayout {
         Layout.preferredHeight: visible ? implicitHeight : 0
         Layout.topMargin: visible ? Tokens.padding.medium : 0
         Layout.rightMargin: Tokens.padding.extraSmall
-        text: qsTr("Ethernet")
+        text: qsTr("有线网络")
         font: Tokens.font.body.builders.medium.weight(Font.Medium).build()
     }
 
@@ -235,7 +235,7 @@ ColumnLayout {
         Layout.preferredHeight: visible ? implicitHeight : 0
         Layout.topMargin: visible ? Tokens.spacing.small : 0
         Layout.rightMargin: Tokens.padding.extraSmall
-        text: qsTr("%1 devices available").arg(Nmcli.ethernetDevices.length)
+        text: qsTr("可用设备：%1 个").arg(Nmcli.ethernetDevices.length)
         color: Colours.palette.m3onSurfaceVariant
         font: Tokens.font.body.small
     }
@@ -289,7 +289,7 @@ ColumnLayout {
                 Layout.leftMargin: Tokens.spacing.extraSmall
                 Layout.rightMargin: Tokens.spacing.extraSmall
                 Layout.fillWidth: true
-                text: ethernetItem.modelData.iface || qsTr("Unknown")
+                text: ethernetItem.modelData.iface || qsTr("未知")
                 elide: Text.ElideRight
                 font: Tokens.font.body.builders.medium.weight(ethernetItem.modelData.connected ? Font.Medium : Font.Normal).build()
                 color: ethernetItem.modelData.connected ? Colours.palette.m3primary : Colours.palette.m3onSurface
diff --git a/modules/bar/popouts/TrayMenu.qml b/modules/bar/popouts/TrayMenu.qml
index 84778f15b01b5dc7e1b2933dd2313e849a335918..240c4f5189d662e5f5ae8de7d530fbb524a184b3 100644
--- a/modules/bar/popouts/TrayMenu.qml
+++ b/modules/bar/popouts/TrayMenu.qml
@@ -219,7 +219,7 @@ StackView {
 
                         StyledText {
                             anchors.verticalCenter: parent.verticalCenter
-                            text: qsTr("Back")
+                            text: qsTr("返回")
                             color: Colours.palette.m3onSecondaryContainer
                         }
                     }
diff --git a/modules/bar/popouts/WirelessPassword.qml b/modules/bar/popouts/WirelessPassword.qml
index 40d159df877d955478c118d326c5387be6010df3..21021c1f8439a13e14da7b56755869709d7d72de 100644
--- a/modules/bar/popouts/WirelessPassword.qml
+++ b/modules/bar/popouts/WirelessPassword.qml
@@ -41,7 +41,7 @@ ColumnLayout {
                 connectButton.connecting = false;
                 connectButton.hasError = true;
                 connectButton.enabled = true;
-                connectButton.text = qsTr("Connect");
+                connectButton.text = qsTr("连接");
                 passwordContainer.passwordBuffer = "";
                 // Delete the failed connection
                 if (root.network && root.network.ssid) {
@@ -60,7 +60,7 @@ ColumnLayout {
         passwordContainer.passwordBuffer = "";
         connectButton.connecting = false;
         connectButton.hasError = false;
-        connectButton.text = qsTr("Connect");
+        connectButton.text = qsTr("连接");
         connectionMonitor.stop();
 
         // Return to network popout
@@ -185,7 +185,7 @@ ColumnLayout {
 
             StyledText {
                 Layout.alignment: Qt.AlignHCenter
-                text: qsTr("Enter password")
+                text: qsTr("输入密码")
                 font: Tokens.font.body.builders.large.weight(Font.Medium).build()
             }
 
@@ -197,10 +197,10 @@ ColumnLayout {
                     if (root.network) {
                         const ssid = root.network.ssid;
                         if (ssid && ssid.length > 0) {
-                            return qsTr("Network: %1").arg(ssid);
+                            return qsTr("网络：%1").arg(ssid);
                         }
                     }
-                    return qsTr("Network: Unknown");
+                    return qsTr("网络：未知");
                 }
                 color: Colours.palette.m3outline
                 font: Tokens.font.body.small
@@ -243,10 +243,10 @@ ColumnLayout {
                 visible: connectButton.connecting || connectButton.hasError
                 text: {
                     if (connectButton.hasError) {
-                        return qsTr("Connection failed. Please check your password and try again.");
+                        return qsTr("连接失败。请检查密码后重试。");
                     }
                     if (connectButton.connecting) {
-                        return qsTr("Connecting...");
+                        return qsTr("正在连接...");
                     }
                     return "";
                 }
@@ -374,7 +374,7 @@ ColumnLayout {
                     id: placeholder
 
                     anchors.centerIn: parent
-                    text: qsTr("Password")
+                    text: qsTr("密码")
                     color: Colours.palette.m3outline
                     font: Tokens.font.mono.medium
                     opacity: passwordContainer.passwordBuffer ? 0 : 1
@@ -479,7 +479,7 @@ ColumnLayout {
                     Layout.minimumHeight: Tokens.font.body.medium.pointSize + Tokens.padding.medium * 2
                     inactiveColour: Colours.palette.m3secondaryContainer
                     inactiveOnColour: Colours.palette.m3onSecondaryContainer
-                    text: qsTr("Cancel")
+                    text: qsTr("取消")
 
                     onClicked: root.closeDialog()
                 }
@@ -494,7 +494,7 @@ ColumnLayout {
                     Layout.minimumHeight: Tokens.font.body.medium.pointSize + Tokens.padding.medium * 2
                     inactiveColour: Colours.palette.m3primary
                     inactiveOnColour: Colours.palette.m3onPrimary
-                    text: qsTr("Connect")
+                    text: qsTr("连接")
                     enabled: passwordContainer.passwordBuffer.length > 0 && !connecting
 
                     onClicked: {
@@ -513,7 +513,7 @@ ColumnLayout {
                         // Set connecting state
                         connecting = true;
                         enabled = false;
-                        text = qsTr("Connecting...");
+                        text = qsTr("正在连接...");
 
                         // Connect to network
                         NetworkConnection.connectWithPassword(root.network, password, result => {
@@ -525,7 +525,7 @@ ColumnLayout {
                                 connecting = false;
                                 hasError = true;
                                 enabled = true;
-                                text = qsTr("Connect");
+                                text = qsTr("连接");
                                 passwordContainer.passwordBuffer = "";
                                 // Delete the failed connection
                                 if (root.network && root.network.ssid) {
@@ -537,7 +537,7 @@ ColumnLayout {
                                 connecting = false;
                                 hasError = true;
                                 enabled = true;
-                                text = qsTr("Connect");
+                                text = qsTr("连接");
                                 passwordContainer.passwordBuffer = "";
                                 // Delete the failed connection
                                 if (root.network && root.network.ssid) {
@@ -586,7 +586,7 @@ ColumnLayout {
                 if (stillConnected) {
                     connectionMonitor.stop();
                     connectButton.connecting = false;
-                    connectButton.text = qsTr("Connect");
+                    connectButton.text = qsTr("连接");
                     // Return to network popout on successful connection
                     if (root.popouts.currentName === "wirelesspassword") {
                         root.popouts.currentName = "network";
@@ -610,7 +610,7 @@ ColumnLayout {
                 connectButton.connecting = false;
                 connectButton.hasError = true;
                 connectButton.enabled = true;
-                connectButton.text = qsTr("Connect");
+                connectButton.text = qsTr("连接");
                 passwordContainer.passwordBuffer = "";
                 // Delete the failed connection
                 Nmcli.forgetNetwork(ssid);
diff --git a/modules/bar/popouts/kblayout/KbLayout.qml b/modules/bar/popouts/kblayout/KbLayout.qml
index abbbae097a3ca9286ab63ad3785f38e46074d954..6fb23f245d291c5dbfda682875441ae1bf436a44 100644
--- a/modules/bar/popouts/kblayout/KbLayout.qml
+++ b/modules/bar/popouts/kblayout/KbLayout.qml
@@ -26,7 +26,7 @@ ColumnLayout {
     StyledText {
         Layout.topMargin: Tokens.padding.medium
         Layout.rightMargin: Tokens.padding.extraSmall
-        text: qsTr("Keyboard Layouts")
+        text: qsTr("键盘布局")
         font: Tokens.font.body.builders.medium.weight(Font.Medium).build()
     }
 
@@ -90,7 +90,7 @@ ColumnLayout {
             width: list.width
             height: Math.max(36, rowText.implicitHeight + Tokens.padding.small)
             ToolTip.visible: isDisabled && layer.containsMouse
-            ToolTip.text: "XKB limitation: maximum 4 layouts allowed"
+            ToolTip.text: "XKB 限制：最多允许 4 个布局"
 
             StateLayer {
                 id: layer
diff --git a/modules/bar/popouts/kblayout/KbLayoutModel.qml b/modules/bar/popouts/kblayout/KbLayoutModel.qml
index 1cd9243e5075d256d3037c52ecdb7fe7ba97b92f..6ca75566442be3c53705237ceec1a163050e6ac5 100644
--- a/modules/bar/popouts/kblayout/KbLayoutModel.qml
+++ b/modules/bar/popouts/kblayout/KbLayoutModel.qml
@@ -110,7 +110,7 @@ Item {
             return;
 
         if (layoutsModel.count > 4) {
-            Toaster.toast(qsTr("Keyboard layout limit"), qsTr("XKB supports only 4 layouts at a time"), "warning");
+            Toaster.toast(qsTr("键盘布局限制"), qsTr("XKB 一次最多支持 4 个布局"), "warning");
         }
     }
 
diff --git a/modules/dashboard/Media.qml b/modules/dashboard/Media.qml
index 2a2f5c2e70cbb1bce716a30b064faae0e110c9f8..80ca5cfc245b1cbb42674023a205f7845a5aec4a 100644
--- a/modules/dashboard/Media.qml
+++ b/modules/dashboard/Media.qml
@@ -114,12 +114,12 @@ Item {
 
                     StyledText {
                         Layout.alignment: Qt.AlignHCenter
-                        text: qsTr("Nothing playing")
+                        text: qsTr("没有正在播放")
                         font: Tokens.font.headline.medium
                     }
 
                     StyledText {
-                        text: qsTr("Play something for it to show up here!")
+                        text: qsTr("播放内容后会显示在这里")
                         color: Colours.palette.m3onSurfaceVariant
                         font: Tokens.font.body.large
                     }
diff --git a/modules/dashboard/Performance.qml b/modules/dashboard/Performance.qml
index 3973bdc0c5a805842c016cb8060792634d7fda92..3520f12e488cf3806b1a8b56a158032767b3782d 100644
--- a/modules/dashboard/Performance.qml
+++ b/modules/dashboard/Performance.qml
@@ -33,14 +33,14 @@ Item {
             StyledText {
                 Layout.topMargin: -Tokens.spacing.small
                 Layout.alignment: Qt.AlignHCenter
-                text: qsTr("No widgets enabled")
+                text: qsTr("未启用组件")
                 font: Tokens.font.title.large
                 color: Colours.palette.m3onSurface
             }
 
             StyledText {
                 Layout.alignment: Qt.AlignHCenter
-                text: qsTr("Enable widgets in the dashboard settings")
+                text: qsTr("请在仪表盘设置中启用组件")
                 font: Tokens.font.body.small
                 color: Colours.palette.m3onSurfaceVariant
             }
diff --git a/modules/dashboard/WeatherTab.qml b/modules/dashboard/WeatherTab.qml
index d2db255544ab0446e3ac67daec665a92b69e3e15..16f562c0b4c1452db2bc72139dd1d8372c978569 100644
--- a/modules/dashboard/WeatherTab.qml
+++ b/modules/dashboard/WeatherTab.qml
@@ -28,7 +28,7 @@ Item {
                 spacing: Tokens.spacing.extraSmall
 
                 StyledText {
-                    text: Weather.city || qsTr("Loading...")
+                    text: Weather.city || qsTr("正在加载...")
                     font: Tokens.font.body.builders.large.size(28).weight(Font.DemiBold).build()
                     color: Colours.palette.m3onSurface
                 }
@@ -49,14 +49,14 @@ Item {
 
                 WeatherStat {
                     icon: "wb_twilight"
-                    label: "Sunrise"
+                    label: "日出"
                     value: Weather.sunrise
                     colour: Colours.palette.m3tertiary
                 }
 
                 WeatherStat {
                     icon: "bedtime"
-                    label: "Sunset"
+                    label: "日落"
                     value: Weather.sunset
                     colour: Colours.palette.m3tertiary
                 }
@@ -110,19 +110,19 @@ Item {
 
             DetailCard {
                 icon: "water_drop"
-                label: "Humidity"
+                label: "湿度"
                 value: Weather.humidity + "%"
                 colour: Colours.palette.m3secondary
             }
             DetailCard {
                 icon: "thermostat"
-                label: "Feels Like"
+                label: "体感温度"
                 value: Weather.feelsLike
                 colour: Colours.palette.m3primary
             }
             DetailCard {
                 icon: "air"
-                label: "Wind"
+                label: "风速"
                 value: Weather.windSpeed ? Weather.windSpeed + " km/h" : "--"
                 colour: Colours.palette.m3tertiary
             }
@@ -132,7 +132,7 @@ Item {
             Layout.topMargin: Tokens.spacing.medium
             Layout.leftMargin: Tokens.padding.medium
             visible: forecastRepeater.count > 0
-            text: qsTr("7-Day Forecast")
+            text: qsTr("7 天预报")
             font: Tokens.font.body.builders.medium.weight(Font.DemiBold).build()
             color: Colours.palette.m3onSurface
         }
@@ -166,7 +166,7 @@ Item {
 
                         StyledText {
                             Layout.alignment: Qt.AlignHCenter
-                            text: forecastItem.index === 0 ? qsTr("Today") : new Date(forecastItem.modelData.date).toLocaleDateString(Qt.locale(), "ddd")
+                            text: forecastItem.index === 0 ? qsTr("今天") : new Date(forecastItem.modelData.date).toLocaleDateString(Qt.locale(), "ddd")
                             font: Tokens.font.body.builders.medium.weight(Font.DemiBold).build()
                             color: Colours.palette.m3primary
                         }
diff --git a/modules/dashboard/Wrapper.qml b/modules/dashboard/Wrapper.qml
index 5debd7adb786589b247e7e95a36bc8450accad97..b256fe04dc19d4103c4bd20fa5a52433332ffc11 100644
--- a/modules/dashboard/Wrapper.qml
+++ b/modules/dashboard/Wrapper.qml
@@ -16,8 +16,8 @@ Item {
         reloadableId: "dashboardState"
     }
     readonly property FileDialog facePicker: FileDialog {
-        title: qsTr("Select a profile picture")
-        filterLabel: qsTr("Image files")
+        title: qsTr("选择头像")
+        filterLabel: qsTr("图片文件")
         filters: Images.validImageExtensions
         onAccepted: path => {
             if (CUtils.copyFile(Qt.resolvedUrl(path), Qt.resolvedUrl(`${Paths.home}/.face`)))
diff --git a/modules/dashboard/dash/Media.qml b/modules/dashboard/dash/Media.qml
index bbd8a3515daf5459360454524a6edcdc8059ce66..f90e940a748b8bc4bd4b356a8cea4ca0c79431c4 100644
--- a/modules/dashboard/dash/Media.qml
+++ b/modules/dashboard/dash/Media.qml
@@ -79,7 +79,7 @@ Item {
 
         animate: true
         horizontalAlignment: Text.AlignHCenter
-        text: (Players.active?.trackTitle ?? qsTr("No media")) || qsTr("Unknown title")
+        text: (Players.active?.trackTitle ?? qsTr("无媒体")) || qsTr("未知标题")
         color: Colours.palette.m3primary
         font: Tokens.font.title.small
 
@@ -96,7 +96,7 @@ Item {
 
         animate: true
         horizontalAlignment: Text.AlignHCenter
-        text: (Players.active?.trackAlbum ?? qsTr("No media")) || qsTr("Unknown album")
+        text: (Players.active?.trackAlbum ?? qsTr("无媒体")) || qsTr("未知专辑")
         color: Colours.palette.m3outline
         font: Tokens.font.body.small
 
@@ -113,7 +113,7 @@ Item {
 
         animate: true
         horizontalAlignment: Text.AlignHCenter
-        text: (Players.active?.trackArtist ?? qsTr("No media")) || qsTr("Unknown artist")
+        text: (Players.active?.trackArtist ?? qsTr("无媒体")) || qsTr("未知艺人")
         color: Colours.palette.m3secondary
 
         width: parent.implicitWidth - Tokens.padding.extraLargeIncreased
diff --git a/modules/dashboard/dash/User.qml b/modules/dashboard/dash/User.qml
index d8d5612866c60b2eb4a48b9eccf2edfda4b497c5..21cba199ae6332f67aa65a195eeccd76bed75e3a 100644
--- a/modules/dashboard/dash/User.qml
+++ b/modules/dashboard/dash/User.qml
@@ -201,7 +201,7 @@ Item {
         anchors.leftMargin: Tokens.spacing.small
         anchors.verticalCenterOffset: Math.round(fontInfo.pointSize * 0.1)
 
-        text: "up " + SysInfo.uptime.split(",").slice(0, 2).join(",") // Max 2 components
+        text: "已运行 " + SysInfo.uptime.split(",").slice(0, 2).join(",") // Max 2 components
         width: Tokens.sizes.dashboard.userWidth - x - Tokens.padding.extraLarge
         elide: Text.ElideRight
     }
diff --git a/modules/dashboard/media/Details.qml b/modules/dashboard/media/Details.qml
index 5288efbe80d11bcd6ee831a4a54aea54fc2ebd48..cbc7b9d21469a5f4d6469c35876a998708eebe81 100644
--- a/modules/dashboard/media/Details.qml
+++ b/modules/dashboard/media/Details.qml
@@ -43,7 +43,7 @@ ColumnLayout {
 
     StyledText {
         Layout.fillWidth: true
-        text: Players.active?.trackArtist || qsTr("Unknown artist")
+        text: Players.active?.trackArtist || qsTr("未知艺人")
         color: Colours.palette.m3onSurfaceVariant
         font: Tokens.font.title.medium
         elide: Text.ElideRight
@@ -52,7 +52,7 @@ ColumnLayout {
 
     StyledText {
         Layout.fillWidth: true
-        text: Players.active?.trackAlbum || qsTr("Unknown album")
+        text: Players.active?.trackAlbum || qsTr("未知专辑")
         color: Colours.palette.m3secondary
         font: Tokens.font.title.medium
         elide: Text.ElideRight
diff --git a/modules/dashboard/media/LyricList.qml b/modules/dashboard/media/LyricList.qml
index c7fa7d251b06cf82cb37fb1dfc4044fa2b123aed..6d754f083fde6975964d6a76628950ec3f4ad3fc 100644
--- a/modules/dashboard/media/LyricList.qml
+++ b/modules/dashboard/media/LyricList.qml
@@ -188,7 +188,7 @@ Item {
             }
 
             StyledText {
-                text: qsTr("Loading lyrics...")
+                text: qsTr("正在加载歌词...")
                 color: Colours.palette.m3onSurfaceVariant
                 font: Tokens.font.title.medium
             }
@@ -220,7 +220,7 @@ Item {
             }
 
             StyledText {
-                text: qsTr("No lyrics found")
+                text: qsTr("未找到歌词")
                 color: Colours.palette.m3outline
                 font: Tokens.font.title.medium
             }
diff --git a/modules/dashboard/media/LyricsAndSelector.qml b/modules/dashboard/media/LyricsAndSelector.qml
index 89d7ce5fbaeea03277441d2a31e30240f0cf93be..92dec55b32d94a9d02bf7b115a4f8bb643e3a93f 100644
--- a/modules/dashboard/media/LyricsAndSelector.qml
+++ b/modules/dashboard/media/LyricsAndSelector.qml
@@ -28,7 +28,7 @@ Item {
 
             StyledText {
                 Layout.fillWidth: true
-                text: qsTr("Lyrics")
+                text: qsTr("歌词")
                 font: Tokens.font.title.medium
             }
 
@@ -50,7 +50,7 @@ Item {
 
             menuItems: playerList.instances
             fallbackIcon: "music_off"
-            fallbackText: qsTr("No players")
+            fallbackText: qsTr("没有播放器")
 
             minLeftWidth: layout.width - expandBtn.implicitWidth - spacing
             label.Layout.maximumWidth: minLeftWidth - iconLabel.implicitWidth - textRow.spacing - textRow.anchors.horizontalCenterOffset / 2 - horizontalPadding * 2
diff --git a/modules/dashboard/media/LyricsInfo.qml b/modules/dashboard/media/LyricsInfo.qml
index 4091f05a971b86ea869b46dbbcd3ff4d120de625..dda6322328227f5cfd5469f9b7936064a2a124ac 100644
--- a/modules/dashboard/media/LyricsInfo.qml
+++ b/modules/dashboard/media/LyricsInfo.qml
@@ -148,21 +148,21 @@ Item {
                 opacity: 0
 
                 StyledText {
-                    text: qsTr("Backend: %1").arg(LyricsBackend.toString(Lyrics.backend))
+                    text: qsTr("后端：%1").arg(LyricsBackend.toString(Lyrics.backend))
                     color: Colours.palette.m3onSurfaceVariant
                     animate: true
                 }
 
                 StyledText {
                     Layout.maximumWidth: Tokens.sizes.dashboard.mediaTabWidth / 2
-                    text: qsTr("Selected candidate: %1 | %2 | %3").arg(Lyrics.selectedCandidate.title).arg(Lyrics.selectedCandidate.artist).arg(Lyrics.selectedCandidate.album)
+                    text: qsTr("已选候选：%1｜%2｜%3").arg(Lyrics.selectedCandidate.title).arg(Lyrics.selectedCandidate.artist).arg(Lyrics.selectedCandidate.album)
                     color: Colours.palette.m3onSurfaceVariant
                     wrapMode: Text.WrapAtWordBoundaryOrAnywhere
                     animate: true
                 }
 
                 StyledText {
-                    text: qsTr("Offset: %1 ms").arg(Lyrics.offset)
+                    text: qsTr("偏移：%1 毫秒").arg(Lyrics.offset)
                     color: Colours.palette.m3onSurfaceVariant
                     animate: true
                 }
@@ -178,7 +178,7 @@ Item {
                 StyledText {
                     id: placeholderText
 
-                    text: Lyrics.loading ? qsTr("Loading...") : qsTr("No lyrics found")
+                    text: Lyrics.loading ? qsTr("正在加载...") : qsTr("未找到歌词")
                     color: Colours.palette.m3onSurfaceVariant
                     font: Tokens.font.body.medium
                     animate: true
diff --git a/modules/dashboard/performance/BatteryTank.qml b/modules/dashboard/performance/BatteryTank.qml
index 691405dbe8a0d3f70ab34f627337e8da5faf8c0b..90b4cdeeba443db185680b730744ea8837a5e394 100644
--- a/modules/dashboard/performance/BatteryTank.qml
+++ b/modules/dashboard/performance/BatteryTank.qml
@@ -74,7 +74,7 @@ StyledClippingRect {
 
         StyledText {
             Layout.fillWidth: true
-            text: qsTr("Battery")
+            text: qsTr("电池")
             color: contents.textColour
             font: Tokens.font.body.medium
         }
@@ -87,10 +87,10 @@ StyledClippingRect {
             Layout.alignment: Qt.AlignRight
             text: {
                 if (UPower.displayDevice.state === UPowerDeviceState.FullyCharged)
-                    return qsTr("Full");
+                    return qsTr("已充满");
 
                 if (contents.charging)
-                    return qsTr("Charging");
+                    return qsTr("充电中");
 
                 const s = UPower.displayDevice.timeToEmpty;
                 if (s === 0)
diff --git a/modules/dashboard/performance/HeroCard.qml b/modules/dashboard/performance/HeroCard.qml
index c3805d5e1fdbf1b499b324bc286a0608c3bba92b..4fa5befd349edee7031286e42ea482376b36b635 100644
--- a/modules/dashboard/performance/HeroCard.qml
+++ b/modules/dashboard/performance/HeroCard.qml
@@ -133,7 +133,7 @@ StyledRect {
             anchors.bottom: parent.top
             anchors.horizontalCenter: parent.horizontalCenter
 
-            text: qsTr("Usage")
+            text: qsTr("使用率")
             color: Colours.palette.m3onSurfaceVariant
             font: Tokens.font.body.small
         }
diff --git a/modules/dashboard/performance/MemoryCard.qml b/modules/dashboard/performance/MemoryCard.qml
index 963e8b9463416929451336084eb9a9658d7a6ade..c00c0a3abed1c5b40c0d122ab77edb49bf7b9648 100644
--- a/modules/dashboard/performance/MemoryCard.qml
+++ b/modules/dashboard/performance/MemoryCard.qml
@@ -39,7 +39,7 @@ StyledRect {
             }
 
             StyledText {
-                text: qsTr("Memory")
+                text: qsTr("内存")
                 font: Tokens.font.title.medium
             }
         }
@@ -74,7 +74,7 @@ StyledRect {
 
                 StyledText {
                     Layout.alignment: Qt.AlignHCenter
-                    text: qsTr("Used")
+                    text: qsTr("已用")
                     font: Tokens.font.body.small
                     color: Colours.palette.m3onSurfaceVariant
                 }
diff --git a/modules/dashboard/performance/NetworkCard.qml b/modules/dashboard/performance/NetworkCard.qml
index 4eccebe74693acfcd08df396d73d16712cd7c0be..4807c790d8d409110a6323b52415fdccaf27883e 100644
--- a/modules/dashboard/performance/NetworkCard.qml
+++ b/modules/dashboard/performance/NetworkCard.qml
@@ -37,7 +37,7 @@ StyledRect {
             }
 
             StyledText {
-                text: qsTr("Network")
+                text: qsTr("网络")
                 font: Tokens.font.title.medium
             }
         }
@@ -93,7 +93,7 @@ StyledRect {
             // "Collecting data" placeholder
             StyledText {
                 anchors.centerIn: parent
-                text: qsTr("Collecting data...")
+                text: qsTr("正在收集数据...")
                 font: Tokens.font.body.small
                 color: Colours.palette.m3outline
                 visible: Native.NetworkUsage.downloadBuffer.count < 2
@@ -112,7 +112,7 @@ StyledRect {
             }
 
             StyledText {
-                text: qsTr("Download")
+                text: qsTr("下载")
                 font: Tokens.font.body.small
                 color: Colours.palette.m3onSurfaceVariant
             }
@@ -143,7 +143,7 @@ StyledRect {
             }
 
             StyledText {
-                text: qsTr("Upload")
+                text: qsTr("上传")
                 font: Tokens.font.body.small
                 color: Colours.palette.m3onSurfaceVariant
             }
@@ -174,7 +174,7 @@ StyledRect {
             }
 
             StyledText {
-                text: qsTr("Total")
+                text: qsTr("总计")
                 font: Tokens.font.body.small
                 color: Colours.palette.m3onSurfaceVariant
             }
diff --git a/modules/dashboard/performance/StorageCard.qml b/modules/dashboard/performance/StorageCard.qml
index 41513ea50a91a951823302539416caef20f78475..0a92404afaf8034c67aa5a8af0fdcaa66062b8ba 100644
--- a/modules/dashboard/performance/StorageCard.qml
+++ b/modules/dashboard/performance/StorageCard.qml
@@ -71,7 +71,7 @@ StyledRect {
 
                     StyledText {
                         Layout.alignment: Qt.AlignHCenter
-                        text: qsTr("Used")
+                        text: qsTr("已用")
                         font: Tokens.font.body.small
                         color: Colours.palette.m3onSurfaceVariant
                     }
@@ -83,14 +83,14 @@ StyledRect {
                 spacing: Tokens.spacing.extraSmall
 
                 StyledText {
-                    text: qsTr("Storage")
+                    text: qsTr("存储")
                     font: Tokens.font.title.medium
                 }
 
                 StyledText {
                     text: {
                         if (!Storage.primaryDisk)
-                            return qsTr("No disks detected");
+                            return qsTr("未检测到磁盘");
 
                         const fmt = UsageFmt.formatKib(Storage.primaryDisk.used, Storage.primaryDisk.total);
                         return `${+fmt.value.toFixed(1)} / ${+fmt.total.toFixed(1)} ${fmt.unit}`;
@@ -107,7 +107,7 @@ StyledRect {
             type: SplitButton.Tonal
             disabled: !Storage.disks.length
             fallbackIcon: "storage"
-            fallbackText: qsTr("No disks")
+            fallbackText: qsTr("无磁盘")
             menuOnTop: true
             minLeftWidth: row.implicitWidth * 0.6
 
diff --git a/modules/launcher/Content.qml b/modules/launcher/Content.qml
index 1c9903884b3fdf4a01591aa2a2b3827cdc30dd32..a5421621dda5f0a33ddf09f728da4b5d80a82785 100644
--- a/modules/launcher/Content.qml
+++ b/modules/launcher/Content.qml
@@ -80,7 +80,7 @@ Item {
             topPadding: Tokens.padding.medium
             bottomPadding: Tokens.padding.medium
 
-            placeholderText: qsTr("Type \"%1\" for commands").arg(GlobalConfig.launcher.actionPrefix)
+            placeholderText: qsTr("输入“%1”使用命令").arg(GlobalConfig.launcher.actionPrefix)
 
             onAccepted: {
                 const currentItem = list.currentList?.currentItem;
diff --git a/modules/launcher/ContentList.qml b/modules/launcher/ContentList.qml
index ff420ce31867dc1d6a372eff8e73514b10cdbcb5..d9bf6ad06de166b52a2aec9db3c4bbc29f2881d9 100644
--- a/modules/launcher/ContentList.qml
+++ b/modules/launcher/ContentList.qml
@@ -129,13 +129,13 @@ Item {
             anchors.verticalCenter: parent.verticalCenter
 
             StyledText {
-                text: root.state === "wallpapers" ? qsTr("No wallpapers found") : qsTr("No results")
+                text: root.state === "wallpapers" ? qsTr("未找到壁纸") : qsTr("无结果")
                 color: Colours.palette.m3onSurfaceVariant
                 font: Tokens.font.body.builders.large.weight(Font.Medium).build()
             }
 
             StyledText {
-                text: root.state === "wallpapers" && Wallpapers.list.length === 0 ? qsTr("Try putting some wallpapers in %1").arg(Paths.shortenHome(Paths.wallsdir)) : qsTr("Try searching for something else")
+                text: root.state === "wallpapers" && Wallpapers.list.length === 0 ? qsTr("请把壁纸放入 %1").arg(Paths.shortenHome(Paths.wallsdir)) : qsTr("尝试搜索其他内容")
                 color: Colours.palette.m3onSurfaceVariant
                 font: Tokens.font.body.medium
             }
diff --git a/modules/launcher/items/CalcItem.qml b/modules/launcher/items/CalcItem.qml
index 53bdde49766d9360af8927f7e7a60354dcace0cd..36bcc80b400ab196500af1f4797c3fbf4d8c225e 100644
--- a/modules/launcher/items/CalcItem.qml
+++ b/modules/launcher/items/CalcItem.qml
@@ -57,7 +57,7 @@ Item {
                 return Colours.palette.m3onSurface;
             }
 
-            text: root.math.length > 0 ? (Qalculator.result || qsTr("Calculating...")) : qsTr("Type an expression to calculate")
+            text: root.math.length > 0 ? (Qalculator.result || qsTr("正在计算...")) : qsTr("输入表达式进行计算")
             elide: Text.ElideLeft
 
             Layout.fillWidth: true
@@ -93,7 +93,7 @@ Item {
                 anchors.right: icon.left
                 anchors.rightMargin: Tokens.spacing.small
 
-                text: qsTr("Open in calculator")
+                text: qsTr("在计算器中打开")
                 color: Colours.palette.m3onTertiary
                 font: Tokens.font.label.medium
 
diff --git a/modules/launcher/services/Actions.qml b/modules/launcher/services/Actions.qml
index dcb48849d3a9fa67b2066377a6088e121fd57c0f..7fee9d2534d2405f649ec47534f25d3a84612090 100644
--- a/modules/launcher/services/Actions.qml
+++ b/modules/launcher/services/Actions.qml
@@ -28,8 +28,8 @@ Searcher {
 
     component Action: QtObject {
         required property var modelData
-        readonly property string name: modelData.name ?? qsTr("Unnamed")
-        readonly property string desc: modelData.description ?? qsTr("No description")
+        readonly property string name: modelData.name ?? qsTr("未命名")
+        readonly property string desc: modelData.description ?? qsTr("无描述")
         readonly property string icon: modelData.icon ?? "help_outline"
         readonly property list<string> command: modelData.command ?? []
         readonly property bool enabled: modelData.enabled ?? true
diff --git a/modules/launcher/services/M3Variants.qml b/modules/launcher/services/M3Variants.qml
index 4517d02cbb8306d9a673ba91c83b46f30131b2d9..2cf88d4047684cc2d69007e6d2dcf923af74e027 100644
--- a/modules/launcher/services/M3Variants.qml
+++ b/modules/launcher/services/M3Variants.qml
@@ -17,56 +17,56 @@ Searcher {
         Variant {
             variant: "vibrant"
             icon: "sentiment_very_dissatisfied"
-            name: qsTr("Vibrant")
-            description: qsTr("A high chroma palette. The primary palette's chroma is at maximum.")
+            name: qsTr("鲜艳")
+            description: qsTr("高彩度配色，主色彩度达到最大值。")
         },
         Variant {
             variant: "tonalspot"
             icon: "android"
-            name: qsTr("Tonal Spot")
-            description: qsTr("Default for Material theme colours. A pastel palette with a low chroma.")
+            name: qsTr("柔和色点")
+            description: qsTr("Material 主题的默认配色，柔和且低彩度。")
         },
         Variant {
             variant: "expressive"
             icon: "compare_arrows"
-            name: qsTr("Expressive")
-            description: qsTr("A medium chroma palette. The primary palette's hue is different from the seed colour, for variety.")
+            name: qsTr("表现力")
+            description: qsTr("中等彩度配色，主色色相与种子色不同，更富变化。")
         },
         Variant {
             variant: "fidelity"
             icon: "compare"
-            name: qsTr("Fidelity")
-            description: qsTr("Matches the seed colour, even if the seed colour is very bright (high chroma).")
+            name: qsTr("保真")
+            description: qsTr("忠实匹配种子色，即使种子色非常鲜艳。")
         },
         Variant {
             variant: "content"
             icon: "sentiment_calm"
-            name: qsTr("Content")
-            description: qsTr("Almost identical to fidelity.")
+            name: qsTr("内容")
+            description: qsTr("与忠实配色几乎相同。")
         },
         Variant {
             variant: "fruitsalad"
             icon: "nutrition"
-            name: qsTr("Fruit Salad")
-            description: qsTr("A playful theme - the seed colour's hue does not appear in the theme.")
+            name: qsTr("水果沙拉")
+            description: qsTr("活泼配色，主题中不直接使用种子色的色相。")
         },
         Variant {
             variant: "rainbow"
             icon: "looks"
-            name: qsTr("Rainbow")
-            description: qsTr("A playful theme - the seed colour's hue does not appear in the theme.")
+            name: qsTr("彩虹")
+            description: qsTr("活泼配色，主题中不直接使用种子色的色相。")
         },
         Variant {
             variant: "neutral"
             icon: "contrast"
-            name: qsTr("Neutral")
-            description: qsTr("Close to grayscale, a hint of chroma.")
+            name: qsTr("中性")
+            description: qsTr("接近灰阶，仅保留少量彩度。")
         },
         Variant {
             variant: "monochrome"
             icon: "filter_b_and_w"
-            name: qsTr("Monochrome")
-            description: qsTr("All colours are grayscale, no chroma.")
+            name: qsTr("单色")
+            description: qsTr("全部使用灰阶，不含彩度。")
         }
     ]
     useFuzzy: GlobalConfig.launcher.useFuzzy.variants
diff --git a/modules/lock/Lock.qml b/modules/lock/Lock.qml
index f852cb7ffd220933dbba4bd1128c70549f3b96da..fb41d4fdebd9764389f6a3dbe47204e2092e86f3 100644
--- a/modules/lock/Lock.qml
+++ b/modules/lock/Lock.qml
@@ -44,7 +44,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "lock"
-        description: "Lock the current session"
+        description: "锁定当前会话"
         onPressed: lock.locked = true
     }
 
@@ -52,7 +52,7 @@ Scope {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "unlock"
-        description: "Unlock the current session"
+        description: "解锁当前会话"
         onPressed: lock.unlock()
     }
 
diff --git a/modules/lock/Media.qml b/modules/lock/Media.qml
index 3a8dc794ac05d907868d372fca62e3cbfa636923..e7fa5d7d77bfb7456ef1bd8b872f1678fefa600e 100644
--- a/modules/lock/Media.qml
+++ b/modules/lock/Media.qml
@@ -56,7 +56,7 @@ StyledClippingRect {
         StyledText {
             Layout.fillWidth: true
             animate: true
-            text: (Players.active?.trackTitle ?? qsTr("Nothing playing")) || qsTr("Unknown track")
+            text: (Players.active?.trackTitle ?? qsTr("暂无播放内容")) || qsTr("未知曲目")
             color: Colours.palette.m3primary
             horizontalAlignment: Text.AlignHCenter
             font: Tokens.font.title.medium
@@ -66,7 +66,7 @@ StyledClippingRect {
         StyledText {
             Layout.fillWidth: true
             animate: true
-            text: (Players.active?.trackArtist ?? qsTr("Try playing some music!")) || qsTr("Unknown artist")
+            text: (Players.active?.trackArtist ?? qsTr("播放一些音乐吧！")) || qsTr("未知艺术家")
             color: Colours.palette.m3onSurfaceVariant
             horizontalAlignment: Text.AlignHCenter
             font: Tokens.font.body.small
diff --git a/modules/lock/NotifDock.qml b/modules/lock/NotifDock.qml
index ed5a396d673fe6001bf4584141cc71866d88bf31..55a7b02d58f38e87eca76a736f5b5d0090720faf 100644
--- a/modules/lock/NotifDock.qml
+++ b/modules/lock/NotifDock.qml
@@ -23,7 +23,7 @@ ColumnLayout {
 
     StyledText {
         Layout.fillWidth: true
-        text: Notifs.list.length > 0 ? qsTr("%1 notification%2").arg(Notifs.list.length).arg(Notifs.list.length === 1 ? "" : "s") : qsTr("Notifications")
+        text: Notifs.list.length > 0 ? qsTr("%1 条通知").arg(Notifs.list.length) : qsTr("通知")
         color: Colours.palette.m3outline
         font: Tokens.font.mono.builders.small.weight(Font.Medium).build()
         elide: Text.ElideRight
@@ -62,7 +62,7 @@ ColumnLayout {
 
                 StyledText {
                     Layout.alignment: Qt.AlignHCenter
-                    text: Config.lock.hideNotifs ? qsTr("Unlock for Notifications") : qsTr("No Notifications")
+                    text: Config.lock.hideNotifs ? qsTr("解锁后查看通知") : qsTr("暂无通知")
                     color: Colours.palette.m3outlineVariant
                     font: Tokens.font.mono.builders.large.weight(Font.Medium).build()
                 }
diff --git a/modules/lock/center/InputField.qml b/modules/lock/center/InputField.qml
index 90b0c7dea683c551fd44b0c55da2afef65028fa6..1f3aecf3c3ace2861a4269f2b8eaf0444dc0fe95 100644
--- a/modules/lock/center/InputField.qml
+++ b/modules/lock/center/InputField.qml
@@ -47,10 +47,10 @@ Item {
 
         text: {
             if (root.pam.passwd.active)
-                return qsTr("Loading...");
+                return qsTr("正在验证…");
             if (root.pam.state === "max")
-                return qsTr("Max tries reached");
-            return qsTr("Enter your password");
+                return qsTr("已达到最大尝试次数");
+            return qsTr("输入密码");
         }
         font: placeholder.font
     }
diff --git a/modules/lock/center/StateMessage.qml b/modules/lock/center/StateMessage.qml
index d6a37a355b26e2563d29d323d0ef373372206958..cb41e2a3cec8a9f0cdb4242d2e676079c5bf24ad 100644
--- a/modules/lock/center/StateMessage.qml
+++ b/modules/lock/center/StateMessage.qml
@@ -13,30 +13,30 @@ Item {
 
     readonly property string msg: {
         if (pam.fprintState === "error")
-            return qsTr("FP ERROR: %1").arg(pam.fprint.message);
+            return qsTr("指纹错误：%1").arg(pam.fprint.message);
         if (pam.state === "error")
-            return qsTr("PW ERROR: %1").arg(pam.passwd.message);
+            return qsTr("密码错误：%1").arg(pam.passwd.message);
 
         if (pam.lockMessage)
             return pam.lockMessage;
 
         if (pam.state === "max" && pam.fprintState === "max")
-            return qsTr("Maximum password and fingerprint attempts reached.");
+            return qsTr("密码和指纹均已达到最大尝试次数。");
         if (pam.state === "max") {
             if (pam.fprint.available)
-                return qsTr("Maximum password attempts reached. Please use fingerprint.");
-            return qsTr("Maximum password attempts reached.");
+                return qsTr("密码已达到最大尝试次数，请使用指纹。");
+            return qsTr("密码已达到最大尝试次数。");
         }
         if (pam.fprintState === "max")
-            return qsTr("Maximum fingerprint attempts reached. Please use password.");
+            return qsTr("指纹已达到最大尝试次数，请使用密码。");
 
         if (pam.state === "fail") {
             if (pam.fprint.available)
-                return qsTr("Incorrect password. Please try again or use fingerprint.");
-            return qsTr("Incorrect password. Please try again.");
+                return qsTr("密码错误，请重试或使用指纹。");
+            return qsTr("密码错误，请重试。");
         }
         if (pam.fprintState === "fail")
-            return qsTr("Fingerprint not recognized (%1/%2). Please try again or use password.").arg(pam.fprint.tries).arg(Config.lock.maxFprintTries);
+            return qsTr("未识别指纹（%1/%2），请重试或使用密码。").arg(pam.fprint.tries).arg(Config.lock.maxFprintTries);
 
         return "";
     }
@@ -44,20 +44,20 @@ Item {
     readonly property string stateMsg: {
         if (Hypr.kbLayout !== Hypr.defaultKbLayout) {
             if (Hypr.capsLock && Hypr.numLock)
-                return qsTr("Caps lock and Num lock are ON.\nKeyboard layout: %1").arg(Hypr.kbLayoutFull);
+                return qsTr("大写锁定和数字锁定已开启。\n键盘布局：%1").arg(Hypr.kbLayoutFull);
             if (Hypr.capsLock)
-                return qsTr("Caps lock is ON. Kb layout: %1").arg(Hypr.kbLayoutFull);
+                return qsTr("大写锁定已开启。键盘布局：%1").arg(Hypr.kbLayoutFull);
             if (Hypr.numLock)
-                return qsTr("Num lock is ON. Kb layout: %1").arg(Hypr.kbLayoutFull);
-            return qsTr("Keyboard layout: %1").arg(Hypr.kbLayoutFull);
+                return qsTr("数字锁定已开启。键盘布局：%1").arg(Hypr.kbLayoutFull);
+            return qsTr("键盘布局：%1").arg(Hypr.kbLayoutFull);
         }
 
         if (Hypr.capsLock && Hypr.numLock)
-            return qsTr("Caps lock and Num lock are ON.");
+            return qsTr("大写锁定和数字锁定已开启。");
         if (Hypr.capsLock)
-            return qsTr("Caps lock is ON.");
+            return qsTr("大写锁定已开启。");
         if (Hypr.numLock)
-            return qsTr("Num lock is ON.");
+            return qsTr("数字锁定已开启。");
 
         return "";
     }
diff --git a/modules/lock/weather/BriefInfo.qml b/modules/lock/weather/BriefInfo.qml
index 7d87dfc17fe388ce08f0d34ede7bbc895b1d177f..a039ff7853cd99c98581539c00aad505a21445a8 100644
--- a/modules/lock/weather/BriefInfo.qml
+++ b/modules/lock/weather/BriefInfo.qml
@@ -44,7 +44,7 @@ ColumnLayout {
         visible: root.rootHeight > Tokens.sizes.lock.showWeatherDetailsHeight
         Layout.alignment: Qt.AlignHCenter
         animate: true
-        text: qsTr("Feels like %1").arg(Weather.temp)
+        text: qsTr("体感温度 %1").arg(Weather.temp)
         color: Colours.palette.m3onSurfaceVariant
         font: Tokens.font.body.large
     }
@@ -55,7 +55,7 @@ ColumnLayout {
         animate: true
         text: {
             const today = Weather.forecast[0];
-            return qsTr("High %1 • Low %2").arg(Weather.formatTemp(today?.maxTempC)).arg(Weather.formatTemp(today?.minTempC));
+            return qsTr("最高 %1 • 最低 %2").arg(Weather.formatTemp(today?.maxTempC)).arg(Weather.formatTemp(today?.minTempC));
         }
         color: Colours.palette.m3onSurfaceVariant
         font: Tokens.font.body.medium
diff --git a/modules/lock/weather/Forecast.qml b/modules/lock/weather/Forecast.qml
index 0ebfd7e2d99cc6daafadae0d0dc061c4bf34eac0..8817e117a721e41f1be99005911a0dcad7b066ae 100644
--- a/modules/lock/weather/Forecast.qml
+++ b/modules/lock/weather/Forecast.qml
@@ -31,7 +31,7 @@ StyledRect {
         StyledText {
             id: title
 
-            text: qsTr("Hourly forecast")
+            text: qsTr("逐小时预报")
             font: Tokens.font.title.medium
         }
     }
@@ -95,7 +95,7 @@ StyledRect {
                 StyledText {
                     Layout.topMargin: Tokens.spacing.extraSmall
                     Layout.alignment: Qt.AlignHCenter
-                    text: hour.index === 0 ? qsTr("Now") : Qt.formatDateTime(new Date(hour.cond.timestamp.replace("T", " ")), GlobalConfig.services.useTwelveHourClock ? "ha" : "hh:00")
+                    text: hour.index === 0 ? qsTr("现在") : Qt.formatDateTime(new Date(hour.cond.timestamp.replace("T", " ")), GlobalConfig.services.useTwelveHourClock ? "ha" : "hh:00")
                     color: Colours.palette.m3onSurfaceVariant
                     font: Tokens.font.body.medium
                 }
diff --git a/modules/nexus/WindowFactory.qml b/modules/nexus/WindowFactory.qml
index ab26d15f9b430cde03d50d9d96a8f1d766497a3a..b17b2ba78516e4484226f3e90f98791286146dde 100644
--- a/modules/nexus/WindowFactory.qml
+++ b/modules/nexus/WindowFactory.qml
@@ -41,7 +41,7 @@ Singleton {
             contentItem.Config.screen: screen.name
             contentItem.Tokens.screen: screen.name
 
-            title: qsTr("Nexus — %1").arg(PageRegistry.pages[nexus.nState.currentPageIdx].label)
+            title: qsTr("设置 — %1").arg(PageRegistry.pages[nexus.nState.currentPageIdx].label)
 
             Nexus {
                 id: nexus
diff --git a/modules/nexus/common/AudioDeviceList.qml b/modules/nexus/common/AudioDeviceList.qml
index d501d628c48f6f6e308bc5da2399098a2ca620ba..f0a71708e8236865fcc6a6822b5ae9b305f05959 100644
--- a/modules/nexus/common/AudioDeviceList.qml
+++ b/modules/nexus/common/AudioDeviceList.qml
@@ -75,7 +75,7 @@ ItemList {
 
             StyledText {
                 Layout.fillWidth: true
-                text: device.modelData?.description || device.modelData?.name || qsTr("Unknown")
+                text: device.modelData?.description || device.modelData?.name || qsTr("未知")
                 font: Tokens.font.body.small
                 elide: Text.ElideRight
             }
diff --git a/modules/nexus/common/EthernetSection.qml b/modules/nexus/common/EthernetSection.qml
index f5cae61c7f3a8928a914f9a61cc4f0861914a525..c3664332b5e69ddf561b1f5bea44707407cbf5cf 100644
--- a/modules/nexus/common/EthernetSection.qml
+++ b/modules/nexus/common/EthernetSection.qml
@@ -50,7 +50,7 @@ ColumnLayout {
 
             StyledText {
                 Layout.fillWidth: true
-                text: qsTr("Ethernet")
+                text: qsTr("有线网络")
                 font: Tokens.font.body.medium
             }
 
@@ -60,7 +60,7 @@ ColumnLayout {
 
                 StyledText {
                     Layout.alignment: Qt.AlignRight
-                    text: Nmcli.activeEthernet ? qsTr("Connected") : qsTr("Not connected")
+                    text: Nmcli.activeEthernet ? qsTr("已连接") : qsTr("未连接")
                     color: Nmcli.activeEthernet ? Colours.palette.m3primary : Colours.palette.m3outline
                     font: Tokens.font.label.small
                 }
@@ -68,7 +68,7 @@ ColumnLayout {
                 StyledText {
                     Layout.alignment: Qt.AlignRight
                     visible: Nmcli.activeEthernet && Nmcli.ethernetDataUsage.length > 0
-                    text: qsTr("Data usage: %1").arg(Nmcli.ethernetDataUsage)
+                    text: qsTr("数据用量：%1").arg(Nmcli.ethernetDataUsage)
                     color: Colours.palette.m3outline
                     font: Tokens.font.label.small
                 }
@@ -138,7 +138,7 @@ ColumnLayout {
 
                     StyledText {
                         Layout.fillWidth: true
-                        text: ethRow.modelData.connection || ethRow.modelData.iface || qsTr("Wired connection")
+                        text: ethRow.modelData.connection || ethRow.modelData.iface || qsTr("有线连接")
                         font: Tokens.font.body.medium
                         elide: Text.ElideRight
                         animate: true
@@ -146,7 +146,7 @@ ColumnLayout {
 
                     StyledText {
                         Layout.fillWidth: true
-                        text: ethRow.isConnected ? ethRow.modelData.iface : qsTr("Not connected • %1").arg(ethRow.modelData.iface)
+                        text: ethRow.isConnected ? ethRow.modelData.iface : qsTr("未连接 • %1").arg(ethRow.modelData.iface)
                         color: ethRow.isConnected ? Colours.palette.m3primary : Colours.palette.m3onSurfaceVariant
                         font: Tokens.font.label.small
                         elide: Text.ElideRight
@@ -190,13 +190,13 @@ ColumnLayout {
                         EthDetail {
                             id: ethIpAddr
 
-                            label: qsTr("Local IP Address")
+                            label: qsTr("本地 IP 地址")
                         }
 
                         EthDetail {
                             id: ethDns
 
-                            label: qsTr("Primary DNS")
+                            label: qsTr("主 DNS")
                         }
                     }
                 }
diff --git a/modules/nexus/navpane/SearchBar.qml b/modules/nexus/navpane/SearchBar.qml
index 6f35061d80182d1495dbfc2a310e8449fa83e6cf..c03944fdc2d24f4516bd1b2f36ff2130b36a2d83 100644
--- a/modules/nexus/navpane/SearchBar.qml
+++ b/modules/nexus/navpane/SearchBar.qml
@@ -49,7 +49,7 @@ StyledRect {
             Layout.fillWidth: true
             Layout.fillHeight: true
 
-            placeholderText: qsTr("Search settings")
+            placeholderText: qsTr("搜索设置")
             placeholderTextColor: Colours.palette.m3onSurfaceVariant
             color: Colours.palette.m3onSurfaceVariant
             font: Tokens.font.body.large
diff --git a/modules/nexus/pages/AboutPage.qml b/modules/nexus/pages/AboutPage.qml
index c3c7b5d5ab7b4fa368e3fc701435a811bca790d1..479801be12115a6e3eb3e1116547eb44b564c33a 100644
--- a/modules/nexus/pages/AboutPage.qml
+++ b/modules/nexus/pages/AboutPage.qml
@@ -17,7 +17,7 @@ PageBase {
     property string quickshellVersion
     property string cliVersion
 
-    title: qsTr("About")
+    title: qsTr("关于")
 
     ColumnLayout {
         anchors.horizontalCenter: parent.horizontalCenter
@@ -85,39 +85,39 @@ PageBase {
 
         // System
         SectionHeader {
-            text: qsTr("System")
+            text: qsTr("系统")
         }
 
         InfoRow {
             first: true
-            label: qsTr("Hostname")
+            label: qsTr("主机名")
             value: SysInfo.hostname
         }
 
         InfoRow {
-            label: qsTr("Device")
+            label: qsTr("设备")
             value: SysInfo.device
         }
 
         InfoRow {
-            label: qsTr("Distro")
+            label: qsTr("发行版")
             value: SysInfo.osPrettyName || SysInfo.osName
         }
 
         InfoRow {
-            label: qsTr("Kernel")
+            label: qsTr("内核")
             value: SysInfo.kernel
         }
 
         InfoRow {
             last: true
-            label: qsTr("Firmware")
+            label: qsTr("固件")
             value: SysInfo.firmware
         }
 
         // Software
         SectionHeader {
-            text: qsTr("Software")
+            text: qsTr("软件")
         }
 
         InfoRow {
@@ -127,7 +127,7 @@ PageBase {
         }
 
         InfoRow {
-            label: qsTr("CLI")
+            label: qsTr("命令行")
             value: root.cliVersion || "…"
         }
 
@@ -144,13 +144,13 @@ PageBase {
 
         // Plugins
         SectionHeader {
-            text: qsTr("Plugins")
+            text: qsTr("插件")
         }
 
         InfoRow {
             first: true
             last: true
-            label: qsTr("Loaded plugins")
+            label: qsTr("已加载插件")
             value: root.pluginCount.toString()
         }
     }
diff --git a/modules/nexus/pages/AppsPage.qml b/modules/nexus/pages/AppsPage.qml
index 2736b1e7b63a283c5a02618c0c0b9dbf5205aa2d..6e59c3a97791c03bc10e67a82a2149d416008b3d 100644
--- a/modules/nexus/pages/AppsPage.qml
+++ b/modules/nexus/pages/AppsPage.qml
@@ -15,7 +15,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("Apps")
+    title: qsTr("应用")
 
     ColumnLayout {
         anchors.horizontalCenter: parent.horizontalCenter
@@ -26,34 +26,34 @@ PageBase {
         // Default applications
         SectionHeader {
             first: true
-            text: qsTr("Default applications")
+            text: qsTr("默认应用")
         }
 
         DefaultRow {
             first: true
             icon: "terminal"
-            label: qsTr("Terminal")
+            label: qsTr("终端")
             status: GlobalConfig.general.apps.terminal.join(" ")
             onSelected: app => GlobalConfig.general.apps.terminal = app.command
         }
 
         DefaultRow {
             icon: "volume_up"
-            label: qsTr("Audio")
+            label: qsTr("音频")
             status: GlobalConfig.general.apps.audio.join(" ")
             onSelected: app => GlobalConfig.general.apps.audio = app.command
         }
 
         DefaultRow {
             icon: "play_circle"
-            label: qsTr("Media playback")
+            label: qsTr("媒体播放")
             status: GlobalConfig.general.apps.playback.join(" ")
             onSelected: app => GlobalConfig.general.apps.playback = app.command
         }
 
         DefaultRow {
             icon: "folder"
-            label: qsTr("File manager")
+            label: qsTr("文件管理器")
             status: GlobalConfig.general.apps.explorer.join(" ")
             onSelected: app => GlobalConfig.general.apps.explorer = app.command
         }
@@ -61,22 +61,22 @@ PageBase {
         DefaultRow {
             last: true
             icon: "language"
-            label: qsTr("Browser")
+            label: qsTr("浏览器")
             status: GlobalConfig.general.apps.browser.join(" ")
             onSelected: app => GlobalConfig.general.apps.browser = app.command
         }
 
         // Library
         SectionHeader {
-            text: qsTr("Library")
+            text: qsTr("应用库")
         }
 
         NavRow {
             first: true
             last: true
             icon: "apps"
-            label: qsTr("All apps")
-            status: qsTr("Browse installed apps, set favourites and hidden")
+            label: qsTr("所有应用")
+            status: qsTr("浏览已安装应用，设置收藏和隐藏")
             onClicked: root.nState.openSubPage(1)
         }
     }
diff --git a/modules/nexus/pages/AudioPage.qml b/modules/nexus/pages/AudioPage.qml
index 09918df010b7e1304ac1dd0a73ad52fc027c9158..8d449a2920b2639c2077cf630f739423d335cbe4 100644
--- a/modules/nexus/pages/AudioPage.qml
+++ b/modules/nexus/pages/AudioPage.qml
@@ -11,7 +11,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("Audio")
+    title: qsTr("音频")
 
     ColumnLayout {
         anchors.horizontalCenter: parent.horizontalCenter
@@ -23,7 +23,7 @@ PageBase {
         SliderRow {
             first: true
             icon: Icons.getVolumeIcon(Audio.volume, Audio.muted)
-            label: qsTr("Output")
+            label: qsTr("输出")
             valueLabel: Math.round(value * 100) + "%"
             value: Audio.volume
             enabled: !Audio.muted
@@ -31,7 +31,7 @@ PageBase {
         }
 
         ToggleRow {
-            text: qsTr("Muted")
+            text: qsTr("已静音")
             checked: Audio.muted
             onToggled: Audio.setStreamMuted(Audio.sink, checked)
         }
@@ -41,7 +41,7 @@ PageBase {
             currentId: Audio.sink?.id ?? -1
             iconName: "speaker"
             placeholderIcon: "speaker"
-            placeholderText: qsTr("No output devices")
+            placeholderText: qsTr("没有输出设备")
             onSelected: node => Audio.setAudioSink(node)
         }
 
@@ -50,7 +50,7 @@ PageBase {
             Layout.topMargin: Tokens.spacing.large - parent.spacing
             first: true
             icon: Icons.getMicVolumeIcon(Audio.sourceVolume, Audio.sourceMuted)
-            label: qsTr("Input")
+            label: qsTr("输入")
             valueLabel: Math.round(value * 100) + "%"
             value: Audio.sourceVolume
             enabled: !Audio.sourceMuted
@@ -58,7 +58,7 @@ PageBase {
         }
 
         ToggleRow {
-            text: qsTr("Muted")
+            text: qsTr("已静音")
             checked: Audio.sourceMuted
             onToggled: Audio.setStreamMuted(Audio.source, checked)
         }
@@ -68,7 +68,7 @@ PageBase {
             currentId: Audio.source?.id ?? -1
             iconName: "mic"
             placeholderIcon: "mic_off"
-            placeholderText: qsTr("No input devices")
+            placeholderText: qsTr("没有输入设备")
             onSelected: node => Audio.setAudioSource(node)
         }
 
@@ -104,14 +104,14 @@ PageBase {
 
                     StyledText {
                         Layout.fillWidth: true
-                        text: qsTr("App volumes")
+                        text: qsTr("应用音量")
                         font: Tokens.font.body.small
                         elide: Text.ElideRight
                     }
 
                     StyledText {
                         Layout.fillWidth: true
-                        text: Audio.streams.length === 0 ? qsTr("No apps playing audio") : Audio.streams.length === 1 ? qsTr("1 app playing audio") : qsTr("%1 apps playing audio").arg(Audio.streams.length)
+                        text: Audio.streams.length === 0 ? qsTr("没有应用正在播放音频") : Audio.streams.length === 1 ? qsTr("1 个应用正在播放音频") : qsTr("%1 个应用正在播放音频").arg(Audio.streams.length)
                         color: Colours.palette.m3outline
                         font: Tokens.font.label.small
                         elide: Text.ElideRight
diff --git a/modules/nexus/pages/BluetoothPage.qml b/modules/nexus/pages/BluetoothPage.qml
index 11e2b1cf3153fc98711df872e0cb4fd866061823..46e05ce6e546d9cdbbeba8ee52edf32c325248c8 100644
--- a/modules/nexus/pages/BluetoothPage.qml
+++ b/modules/nexus/pages/BluetoothPage.qml
@@ -17,7 +17,7 @@ PageBase {
     readonly property BluetoothAdapter adapter: Bluetooth.defaultAdapter // qmllint disable unresolved-type
     readonly property bool btEnabled: adapter?.enabled ?? false
 
-    title: qsTr("Connected devices")
+    title: qsTr("已连接设备")
 
     ColumnLayout {
         anchors.horizontalCenter: parent.horizontalCenter
@@ -27,7 +27,7 @@ PageBase {
 
         ToggleRow {
             first: true
-            text: qsTr("Bluetooth")
+            text: qsTr("蓝牙")
             font: Tokens.font.body.medium
             horizontalPadding: Tokens.padding.largeIncreased
             checked: root.btEnabled
@@ -42,7 +42,7 @@ PageBase {
 
             showList: root.btEnabled
             placeholderIcon: root.btEnabled ? "devices_other" : "bluetooth_disabled"
-            placeholderText: root.btEnabled ? qsTr("No saved devices") : qsTr("Bluetooth disabled")
+            placeholderText: root.btEnabled ? qsTr("没有已保存设备") : qsTr("蓝牙已关闭")
 
             model: ScriptModel {
                 values: Bluetooth.devices.values.filter(d => d.bonded).sort((a, b) => (b.connected - a.connected) || a.name.localeCompare(b.name)) // qmllint disable unresolved-type
@@ -115,14 +115,14 @@ PageBase {
 
                         StyledText {
                             Layout.fillWidth: true
-                            text: device.modelData?.name ?? qsTr("Unknown")
+                            text: device.modelData?.name ?? qsTr("未知")
                             font: Tokens.font.body.small
                             elide: Text.ElideRight
                         }
 
                         StyledText {
                             Layout.fillWidth: true
-                            text: device.connected ? qsTr("Connected%1").arg(device.modelData?.batteryAvailable ? " • " + Math.round(device.modelData.battery * 100) + "%" : "") : qsTr("Saved")
+                            text: device.connected ? qsTr("已连接%1").arg(device.modelData?.batteryAvailable ? " • " + Math.round(device.modelData.battery * 100) + "%" : "") : qsTr("已保存")
                             color: Colours.palette.m3outline
                             font: Tokens.font.label.small
                             elide: Text.ElideRight
@@ -199,7 +199,7 @@ PageBase {
 
                 StyledText {
                     Layout.fillWidth: true
-                    text: qsTr("Pair new device")
+                    text: qsTr("配对新设备")
                     font: Tokens.font.body.small
                     elide: Text.ElideRight
                 }
@@ -210,8 +210,8 @@ PageBase {
             Layout.topMargin: Tokens.spacing.large - parent.spacing
 
             first: true
-            text: qsTr("Discoverable")
-            subtext: qsTr("Allow nearby devices to find this one")
+            text: qsTr("可被发现")
+            subtext: qsTr("允许附近设备发现本机")
             enabled: root.btEnabled
             opacity: root.btEnabled ? 1 : 0.5
             checked: root.adapter?.discoverable ?? false
@@ -227,8 +227,8 @@ PageBase {
 
         ToggleRow {
             last: true
-            text: qsTr("Pairable")
-            subtext: qsTr("Allow nearby devices to pair with this one")
+            text: qsTr("允许配对")
+            subtext: qsTr("允许附近设备与本机配对")
             enabled: root.btEnabled
             opacity: root.btEnabled ? 1 : 0.5
             checked: root.adapter?.pairable ?? false
diff --git a/modules/nexus/pages/PanelsPage.qml b/modules/nexus/pages/PanelsPage.qml
index d49879bec3bb6aea6628aea6bbb66c22e5eacace..52c80a503f75c085d6c99e971f2c340697ab3d6b 100644
--- a/modules/nexus/pages/PanelsPage.qml
+++ b/modules/nexus/pages/PanelsPage.qml
@@ -5,7 +5,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("Panels")
+    title: qsTr("面板")
 
     ColumnLayout {
         anchors.horizontalCenter: parent.horizontalCenter
@@ -16,30 +16,30 @@ PageBase {
         NavRow {
             first: true
             icon: "dashboard"
-            label: qsTr("Dashboard")
-            status: Config.dashboard.enabled ? qsTr("Enabled") : qsTr("Disabled")
+            label: qsTr("仪表盘")
+            status: Config.dashboard.enabled ? qsTr("启用") : qsTr("禁用")
             onClicked: root.nState.openSubPage(1)
         }
 
         NavRow {
             icon: "dock_to_bottom"
-            label: qsTr("Taskbar")
-            status: Config.bar.persistent ? qsTr("Always visible") : Config.bar.showOnHover ? qsTr("Reveal on hover") : qsTr("Reveal on drag")
+            label: qsTr("任务栏")
+            status: Config.bar.persistent ? qsTr("始终可见") : Config.bar.showOnHover ? qsTr("悬停显示") : qsTr("拖动显示")
             onClicked: root.nState.openSubPage(2)
         }
 
         NavRow {
             icon: "apps"
-            label: qsTr("Launcher")
-            status: Config.launcher.enabled ? qsTr("Enabled") : qsTr("Disabled")
+            label: qsTr("启动器")
+            status: Config.launcher.enabled ? qsTr("启用") : qsTr("禁用")
             onClicked: root.nState.openSubPage(3)
         }
 
         NavRow {
             last: true
             icon: "dock_to_right"
-            label: qsTr("Sidebar")
-            status: Config.sidebar.enabled ? qsTr("Enabled") : qsTr("Disabled")
+            label: qsTr("侧边栏")
+            status: Config.sidebar.enabled ? qsTr("启用") : qsTr("禁用")
             onClicked: root.nState.openSubPage(4)
         }
     }
diff --git a/modules/nexus/pages/ServicesPage.qml b/modules/nexus/pages/ServicesPage.qml
index c8ea204526ddedfb8865bf890ae2d54d765006fa..fcf1f47983e6d972aa82cac3162f0836b2bd05d5 100644
--- a/modules/nexus/pages/ServicesPage.qml
+++ b/modules/nexus/pages/ServicesPage.qml
@@ -13,10 +13,10 @@ PageBase {
     // Lyrics backends, ordered to match LyricsBackend::Backend (Auto, Local, LRCLIB, NetEase)
     readonly property list<MenuItem> lyricsItems: [
         MenuItem {
-            text: qsTr("Auto")
+            text: qsTr("自动")
         },
         MenuItem {
-            text: "Local"
+            text: "本地"
         },
         MenuItem {
             text: "LRCLIB"
@@ -29,16 +29,16 @@ PageBase {
     // GPU options + the config string each maps to (see Gpu::parseType)
     readonly property list<MenuItem> gpuItems: [
         MenuItem {
-            text: qsTr("Auto")
+            text: qsTr("自动")
         },
         MenuItem {
             text: "NVIDIA"
         },
         MenuItem {
-            text: qsTr("Generic")
+            text: qsTr("通用")
         },
         MenuItem {
-            text: qsTr("None")
+            text: qsTr("无")
         }
     ]
     readonly property list<string> gpuValues: ["", "NVIDIA", "GENERIC", "None"]
@@ -54,7 +54,7 @@ PageBase {
         return 3; // None
     }
 
-    title: qsTr("Services")
+    title: qsTr("服务")
 
     ColumnLayout {
         anchors.horizontalCenter: parent.horizontalCenter
@@ -80,27 +80,27 @@ PageBase {
         // Notifications
         SectionHeader {
             first: true
-            text: qsTr("Notifications")
+            text: qsTr("通知")
         }
 
         NavRow {
             first: true
             last: true
             icon: "notifications"
-            label: qsTr("Notifications")
-            status: qsTr("Notifications, toasts, timeouts")
+            label: qsTr("通知")
+            status: qsTr("通知、提示、超时")
             onClicked: root.nState.openSubPage(1)
         }
 
         // Polling
         SectionHeader {
-            text: qsTr("Polling")
+            text: qsTr("轮询")
         }
 
         StepperRow {
             first: true
-            label: qsTr("Media refresh")
-            subtext: qsTr("How often the media position updates (ms)")
+            label: qsTr("媒体刷新")
+            subtext: qsTr("媒体播放位置更新频率（毫秒）")
             value: GlobalConfig.dashboard.mediaUpdateInterval
             from: 100
             to: 2000
@@ -109,8 +109,8 @@ PageBase {
         }
 
         StepperRow {
-            label: qsTr("System stats refresh")
-            subtext: qsTr("CPU, memory and GPU update interval (seconds)")
+            label: qsTr("系统状态刷新")
+            subtext: qsTr("CPU、内存和 GPU 更新间隔（秒）")
             value: GlobalConfig.dashboard.resourceUpdateInterval / 1000
             from: 0.5
             to: 10
@@ -120,8 +120,8 @@ PageBase {
 
         StepperRow {
             last: true
-            label: qsTr("Wi-Fi rescan")
-            subtext: qsTr("How often available networks are rescanned (seconds)")
+            label: qsTr("无线网络重新扫描")
+            subtext: qsTr("可用网络重新扫描频率（秒）")
             value: GlobalConfig.nexus.networkRescanInterval / 1000
             from: 5
             to: 120
@@ -131,13 +131,13 @@ PageBase {
 
         // Media & lyrics
         SectionHeader {
-            text: qsTr("Media & lyrics")
+            text: qsTr("媒体和歌词")
         }
 
         SelectRow {
             first: true
-            label: qsTr("Lyrics backend")
-            subtext: qsTr("Source used to fetch synced lyrics")
+            label: qsTr("歌词后端")
+            subtext: qsTr("用于获取同步歌词的来源")
             menuItems: root.lyricsItems
             active: root.lyricsItems[Lyrics.preferredBackend] ?? root.lyricsItems[0]
             onSelected: item => Lyrics.preferredBackend = root.lyricsItems.indexOf(item)
@@ -145,24 +145,24 @@ PageBase {
 
         SelectRow {
             last: true
-            label: qsTr("Default player")
-            subtext: qsTr("Preferred media player when several are open")
+            label: qsTr("默认播放器")
+            subtext: qsTr("多个播放器打开时优先使用的播放器")
             menuItems: playerVariants.instances
             active: menuItems.find(i => i.text === GlobalConfig.services.defaultPlayer) ?? null
             fallbackIcon: "music_note"
-            fallbackText: GlobalConfig.services.defaultPlayer || qsTr("Auto")
+            fallbackText: GlobalConfig.services.defaultPlayer || qsTr("自动")
             onSelected: item => GlobalConfig.services.defaultPlayer = item.text
         }
 
         // Input increments
         SectionHeader {
-            text: qsTr("Input increments")
+            text: qsTr("输入步进")
         }
 
         StepperRow {
             first: true
-            label: qsTr("Volume step")
-            subtext: qsTr("Amount the volume changes per scroll (%)")
+            label: qsTr("音量步进")
+            subtext: qsTr("每次滚动改变的音量（%）")
             value: Math.round(GlobalConfig.services.audioIncrement * 100)
             from: 1
             to: 50
@@ -171,8 +171,8 @@ PageBase {
         }
 
         StepperRow {
-            label: qsTr("Brightness step")
-            subtext: qsTr("Amount the brightness changes per scroll (%)")
+            label: qsTr("亮度步进")
+            subtext: qsTr("每次滚动改变的亮度（%）")
             value: Math.round(GlobalConfig.services.brightnessIncrement * 100)
             from: 1
             to: 50
@@ -182,8 +182,8 @@ PageBase {
 
         StepperRow {
             last: true
-            label: qsTr("Max volume")
-            subtext: qsTr("Upper limit for output volume (%)")
+            label: qsTr("最大音量")
+            subtext: qsTr("输出音量上限（%）")
             value: Math.round(GlobalConfig.services.maxVolume * 100)
             from: 50
             to: 200
@@ -193,13 +193,13 @@ PageBase {
 
         // Service tuning
         SectionHeader {
-            text: qsTr("Service tuning")
+            text: qsTr("服务调优")
         }
 
         StepperRow {
             first: true
-            label: qsTr("Visualiser bars")
-            subtext: qsTr("Number of bars in the audio visualisers")
+            label: qsTr("可视化条数")
+            subtext: qsTr("音频可视化中的条数")
             value: GlobalConfig.services.visualiserBars
             from: 10
             to: 120
@@ -208,8 +208,8 @@ PageBase {
         }
 
         ToggleRow {
-            text: qsTr("Smart colour scheme")
-            subtext: qsTr("Derive theme mode and variant from the wallpaper")
+            text: qsTr("智能配色方案")
+            subtext: qsTr("根据壁纸生成主题模式和变体")
             checked: GlobalConfig.services.smartScheme
             onToggled: GlobalConfig.services.smartScheme = checked
         }
@@ -217,7 +217,7 @@ PageBase {
         SelectRow {
             last: true
             label: qsTr("GPU")
-            subtext: Gpu.name ? qsTr("Monitoring: %1").arg(Gpu.name) : qsTr("Override for GPU type")
+            subtext: Gpu.name ? qsTr("正在监控：%1").arg(Gpu.name) : qsTr("覆盖 GPU 类型")
             menuOnTop: true
             menuItems: root.gpuItems
             active: root.gpuItems[root.gpuKeyToIndex(GlobalConfig.services.gpuType)]
diff --git a/modules/nexus/pages/apps/AllApps.qml b/modules/nexus/pages/apps/AllApps.qml
index 0102715e92cefa50efd09ecab476e21504e42be2..75df2d4dc898dd84a493a1ff798a64c405af7749 100644
--- a/modules/nexus/pages/apps/AllApps.qml
+++ b/modules/nexus/pages/apps/AllApps.qml
@@ -13,7 +13,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("All apps")
+    title: qsTr("所有应用")
     isSubPage: true
 
     ColumnLayout {
diff --git a/modules/nexus/pages/apps/AppInfo.qml b/modules/nexus/pages/apps/AppInfo.qml
index 0f24f99d773dea122ce343eaf8d01de0abb95413..b827298d3a99d2c3d705f22ba7f2e950fcb43cd1 100644
--- a/modules/nexus/pages/apps/AppInfo.qml
+++ b/modules/nexus/pages/apps/AppInfo.qml
@@ -29,7 +29,7 @@ PageBase {
             nState.closeSubPage();
     }
 
-    title: qsTr("App info")
+    title: qsTr("应用信息")
     isSubPage: true
 
     ColumnLayout {
@@ -76,13 +76,13 @@ PageBase {
         // Launcher
         SectionHeader {
             first: true
-            text: qsTr("Launcher")
+            text: qsTr("启动器")
         }
 
         ToggleRow {
             first: true
-            text: qsTr("Favourite")
-            subtext: root.favouriteByRegex ? qsTr("Matched by a regex in favouriteApps — edit the config file to change") : qsTr("Pin to the top of the launcher")
+            text: qsTr("收藏")
+            subtext: root.favouriteByRegex ? qsTr("由 favouriteApps 中的正则匹配，需编辑配置文件修改") : qsTr("固定到启动器顶部")
             enabled: !root.favouriteByRegex
             checked: root.app && Strings.testRegexList(GlobalConfig.launcher.favouriteApps, root.app.id)
             onToggled: {
@@ -93,8 +93,8 @@ PageBase {
 
         ToggleRow {
             last: true
-            text: qsTr("Hidden")
-            subtext: root.hiddenByRegex ? qsTr("Matched by a regex in hiddenApps — edit the config file to change") : qsTr("Hide from the launcher")
+            text: qsTr("隐藏")
+            subtext: root.hiddenByRegex ? qsTr("由 hiddenApps 中的正则匹配，需编辑配置文件修改") : qsTr("从启动器隐藏")
             enabled: !root.hiddenByRegex
             checked: root.app && Strings.testRegexList(GlobalConfig.launcher.hiddenApps, root.app.id)
             onToggled: {
@@ -105,14 +105,14 @@ PageBase {
 
         // Details
         SectionHeader {
-            text: qsTr("Details")
+            text: qsTr("详情")
         }
 
         WrapInfoRow {
             id: appId
 
             first: true
-            label: qsTr("App ID")
+            label: qsTr("应用 ID")
             value: root.app?.id ?? ""
             labelComp.Layout.preferredWidth: Math.max(labelComp.implicitWidth, command.labelComp.implicitWidth)
         }
@@ -121,7 +121,7 @@ PageBase {
             id: command
 
             last: true
-            label: qsTr("Command")
+            label: qsTr("命令")
             value: (root.app?.command ?? []).join(" ")
             labelComp.Layout.preferredWidth: Math.max(labelComp.implicitWidth, appId.labelComp.implicitWidth)
         }
diff --git a/modules/nexus/pages/audio/AppVolumes.qml b/modules/nexus/pages/audio/AppVolumes.qml
index 8f36a4ca58760d2026685f7aa5d1dd4a07b1b9d6..42fb13243332a97a3ddc2432e6e91cf93067ee56 100644
--- a/modules/nexus/pages/audio/AppVolumes.qml
+++ b/modules/nexus/pages/audio/AppVolumes.qml
@@ -13,7 +13,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("App volumes")
+    title: qsTr("应用音量")
     isSubPage: true
 
     ColumnLayout {
@@ -26,7 +26,7 @@ PageBase {
             Layout.fillWidth: true
             Layout.leftMargin: Tokens.padding.small
             Layout.bottomMargin: Tokens.spacing.medium
-            text: qsTr("Adjust the volume of individual apps currently playing audio.")
+            text: qsTr("调整当前正在播放音频的各个应用音量。")
             color: Colours.palette.m3outline
             font: Tokens.font.body.small
             wrapMode: Text.WordWrap
@@ -39,7 +39,7 @@ PageBase {
             last: true
             showList: true
             placeholderIcon: "music_off"
-            placeholderText: qsTr("No apps playing audio")
+            placeholderText: qsTr("没有应用正在播放音频")
             color: list.count === 0 ? Colours.tPalette.m3surfaceContainer : "transparent"
             list.spacing: Tokens.spacing.extraSmall / 2
 
diff --git a/modules/nexus/pages/bluetooth/BluetoothPairing.qml b/modules/nexus/pages/bluetooth/BluetoothPairing.qml
index 543c967708c29cba819fb66bb1d6d09f9d4b090b..05d6071efd20d7b629638c2f2004cbb967d7e4c7 100644
--- a/modules/nexus/pages/bluetooth/BluetoothPairing.qml
+++ b/modules/nexus/pages/bluetooth/BluetoothPairing.qml
@@ -21,7 +21,7 @@ PageBase {
             adapter.discovering = on;
     }
 
-    title: qsTr("Pair new device")
+    title: qsTr("配对新设备")
     isSubPage: true
 
     Component.onCompleted: setScan(true)
@@ -56,7 +56,7 @@ PageBase {
                 anchors.leftMargin: Tokens.padding.large
                 anchors.verticalCenterOffset: Math.round(fontInfo.pointSize * 0.2)
 
-                text: qsTr("Available devices")
+                text: qsTr("可用设备")
                 color: Colours.palette.m3onSurfaceVariant
                 font: Tokens.font.body.small
             }
@@ -70,7 +70,7 @@ PageBase {
             extraHeight: scanIndicator.implicitHeight
             last: true
             placeholderIcon: "bluetooth_searching"
-            placeholderText: qsTr("Searching for devices…")
+            placeholderText: qsTr("正在搜索设备…")
             list.anchors.top: scanIndicator.bottom
 
             model: ScriptModel {
@@ -139,14 +139,14 @@ PageBase {
 
                         StyledText {
                             Layout.fillWidth: true
-                            text: newDevice.modelData?.name || qsTr("Unknown device")
+                            text: newDevice.modelData?.name || qsTr("未知设备")
                             font: Tokens.font.body.small
                             elide: Text.ElideRight
                         }
 
                         StyledText {
                             Layout.fillWidth: true
-                            text: newDevice.modelData?.pairing ? qsTr("Pairing...") : (newDevice.modelData?.address ?? "")
+                            text: newDevice.modelData?.pairing ? qsTr("正在配对...") : (newDevice.modelData?.address ?? "")
                             color: Colours.palette.m3outline
                             font: Tokens.font.label.small
                             elide: Text.ElideRight
diff --git a/modules/nexus/pages/bluetooth/BtDeviceInfo.qml b/modules/nexus/pages/bluetooth/BtDeviceInfo.qml
index d552cf5d83443cadd48c3a4a228178ce7d0a439b..31fa1b3d6946d3466f0b468684af36e2cd115cf4 100644
--- a/modules/nexus/pages/bluetooth/BtDeviceInfo.qml
+++ b/modules/nexus/pages/bluetooth/BtDeviceInfo.qml
@@ -20,7 +20,7 @@ PageBase {
     readonly property string statusText: {
         if (!device)
             return "";
-        let s = connected ? qsTr("Connected") : (device.bonded ? qsTr("Paired") : qsTr("Not paired"));
+        let s = connected ? qsTr("已连接") : (device.bonded ? qsTr("已配对") : qsTr("未配对"));
         if (connected && device.batteryAvailable)
             s += " • " + Math.round(device.battery * 100) + "%";
         return s;
@@ -32,7 +32,7 @@ PageBase {
             nState.closeSubPage();
     }
 
-    title: device?.name ?? qsTr("Device")
+    title: device?.name ?? qsTr("设备")
     isSubPage: true
 
     ColumnLayout {
@@ -81,7 +81,7 @@ PageBase {
 
                     StyledText {
                         Layout.alignment: Qt.AlignHCenter
-                        text: qsTr("Forget")
+                        text: qsTr("忘记")
                         color: forgetBtn.onColour
                     }
                 }
@@ -136,7 +136,7 @@ PageBase {
 
                         StyledText {
                             Layout.alignment: Qt.AlignHCenter
-                            text: root.connected ? qsTr("Disconnect") : qsTr("Connect")
+                            text: root.connected ? qsTr("断开") : qsTr("连接")
                             color: connectBtn.inactiveOnColour
                             animate: true
                         }
@@ -149,8 +149,8 @@ PageBase {
         ToggleRow {
             verticalPadding: Tokens.padding.large
             first: true
-            text: qsTr("Trusted")
-            subtext: qsTr("Allow this device to connect automatically")
+            text: qsTr("信任")
+            subtext: qsTr("允许此设备自动连接")
             checked: root.device?.trusted ?? false
             onToggled: {
                 if (root.device)
@@ -160,8 +160,8 @@ PageBase {
 
         ToggleRow {
             verticalPadding: Tokens.padding.large
-            text: qsTr("Blocked")
-            subtext: qsTr("Prevent this device from connecting")
+            text: qsTr("已阻止")
+            subtext: qsTr("阻止此设备连接")
             checked: root.device?.blocked ?? false
             onToggled: {
                 if (root.device)
@@ -172,8 +172,8 @@ PageBase {
         ToggleRow {
             verticalPadding: Tokens.padding.large
             last: true
-            text: qsTr("Wake allowed")
-            subtext: qsTr("Allow this device to wake the system")
+            text: qsTr("允许唤醒")
+            subtext: qsTr("允许此设备唤醒系统")
             checked: root.device?.wakeAllowed ?? false
             onToggled: {
                 if (root.device)
@@ -203,11 +203,11 @@ PageBase {
 
                     StyledText {
                         Layout.fillWidth: true
-                        text: qsTr("Battery")
+                        text: qsTr("电池")
                     }
 
                     StyledText {
-                        text: root.device?.batteryAvailable ? Math.round(root.device.battery * 100) + "%" : qsTr("Unavailable")
+                        text: root.device?.batteryAvailable ? Math.round(root.device.battery * 100) + "%" : qsTr("不可用")
                         color: Colours.palette.m3outline
                         font: Tokens.font.body.small
                     }
@@ -243,7 +243,7 @@ PageBase {
 
                 StyledText {
                     Layout.fillWidth: true
-                    text: qsTr("Address")
+                    text: qsTr("地址")
                 }
 
                 StyledText {
diff --git a/modules/nexus/pages/network/EthernetDetailPage.qml b/modules/nexus/pages/network/EthernetDetailPage.qml
index 8c9b4369cc0ec541d882370268d35798207db76a..dd12f18e6a302781796f25119666c193eebc234d 100644
--- a/modules/nexus/pages/network/EthernetDetailPage.qml
+++ b/modules/nexus/pages/network/EthernetDetailPage.qml
@@ -77,7 +77,7 @@ PageBase {
         });
     }
 
-    title: root.device?.connection || root.ifaceName || qsTr("Ethernet")
+    title: root.device?.connection || root.ifaceName || qsTr("有线网络")
     isSubPage: true
 
     Component.onCompleted: {
@@ -133,7 +133,7 @@ PageBase {
 
                     StyledText {
                         Layout.alignment: Qt.AlignHCenter
-                        text: root.device?.connected ? qsTr("Disconnect") : qsTr("Connect")
+                        text: root.device?.connected ? qsTr("断开") : qsTr("连接")
                         color: connectBtn.onColour
                     }
                 }
@@ -143,45 +143,45 @@ PageBase {
         // ---- Connection info ------------------------------------------------
         SectionHeader {
             first: true
-            text: qsTr("Connection")
+            text: qsTr("连接")
         }
 
         InfoRow {
             first: true
             icon: "link"
-            label: qsTr("Status")
-            value: root.device?.connected ? qsTr("Connected") : qsTr("Not connected")
+            label: qsTr("状态")
+            value: root.device?.connected ? qsTr("已连接") : qsTr("未连接")
         }
 
         InfoRow {
             icon: "settings_ethernet"
-            label: qsTr("Interface")
+            label: qsTr("接口")
             value: root.ifaceName || qsTr("—")
         }
 
         InfoRow {
             icon: "speed"
-            label: qsTr("Speed")
+            label: qsTr("速度")
             visible: Nmcli.ethernetSpeed.length > 0
             value: Nmcli.ethernetSpeed
         }
 
         InfoRow {
             icon: "lan"
-            label: qsTr("IP address")
+            label: qsTr("IP 地址")
             value: root.details?.ipAddress || qsTr("—")
         }
 
         InfoRow {
             icon: "router"
-            label: qsTr("Gateway")
+            label: qsTr("网关")
             value: root.details?.gateway || qsTr("—")
         }
 
         InfoRow {
             last: true
             icon: "memory"
-            label: qsTr("MAC address")
+            label: qsTr("MAC 地址")
             value: root.details?.macAddress || qsTr("—")
         }
 
@@ -234,11 +234,11 @@ PageBase {
 
                 Layout.fillWidth: true
                 visible: root.ipMethod === "manual"
-                label: qsTr("Address (CIDR)")
+                label: qsTr("地址（CIDR）")
                 placeholder: qsTr("192.168.1.50/24")
                 leadingIcon: "router"
-                supportingText: qsTr("IP and prefix, e.g. 192.168.1.50/24")
-                errorText: qsTr("Enter a valid address in CIDR notation")
+                supportingText: qsTr("IP 和前缀，例如 192.168.1.50/24")
+                errorText: qsTr("请输入有效的 CIDR 地址")
                 inputMethodHints: Qt.ImhNoPredictiveText
             }
 
@@ -247,7 +247,7 @@ PageBase {
 
                 Layout.fillWidth: true
                 visible: root.ipMethod === "manual"
-                label: qsTr("Gateway")
+                label: qsTr("网关")
                 placeholder: qsTr("192.168.1.1")
                 leadingIcon: "exit_to_app"
                 inputMethodHints: Qt.ImhNoPredictiveText
@@ -257,11 +257,11 @@ PageBase {
                 id: dnsField
 
                 Layout.fillWidth: true
-                label: qsTr("DNS servers")
+                label: qsTr("DNS 服务器")
                 placeholder: qsTr("1.1.1.1, 8.8.8.8")
                 leadingIcon: "dns"
-                supportingText: qsTr("Comma-separated")
-                errorText: qsTr("Enter valid DNS server addresses")
+                supportingText: qsTr("用逗号分隔")
+                errorText: qsTr("请输入有效的 DNS 服务器地址")
                 inputMethodHints: Qt.ImhNoPredictiveText
             }
         }
@@ -315,7 +315,7 @@ PageBase {
                     id: applyTextComp
 
                     StyledText {
-                        text: qsTr("Apply")
+                        text: qsTr("应用")
                         color: applyBtn.onColour
                         animate: true
                     }
diff --git a/modules/nexus/pages/panels/DashboardPanel.qml b/modules/nexus/pages/panels/DashboardPanel.qml
index e168f124bb1df669117a4dd10fb9508d246c620c..cfe5534fac598da256cccfc757e5215ad96ad9a4 100644
--- a/modules/nexus/pages/panels/DashboardPanel.qml
+++ b/modules/nexus/pages/panels/DashboardPanel.qml
@@ -8,7 +8,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("Dashboard")
+    title: qsTr("仪表盘")
     isSubPage: true
 
     ColumnLayout {
@@ -20,63 +20,63 @@ PageBase {
         // General
         SectionHeader {
             first: true
-            text: qsTr("General")
+            text: qsTr("通用")
         }
 
         ToggleRow {
             first: true
-            text: qsTr("Enabled")
+            text: qsTr("启用")
             checked: Config.dashboard.enabled
             onToggled: GlobalConfig.dashboard.enabled = checked
         }
 
         ToggleRow {
             last: true
-            text: qsTr("Show on hover")
-            subtext: qsTr("Reveal when the cursor reaches the screen edge")
+            text: qsTr("悬停显示")
+            subtext: qsTr("光标到达屏幕边缘时显示")
             checked: Config.dashboard.showOnHover
             onToggled: GlobalConfig.dashboard.showOnHover = checked
         }
 
         // Tabs
         SectionHeader {
-            text: qsTr("Tabs")
+            text: qsTr("标签页")
         }
 
         ToggleRow {
             first: true
-            text: qsTr("Dashboard")
+            text: qsTr("仪表盘")
             checked: Config.dashboard.showDashboard
             onToggled: GlobalConfig.dashboard.showDashboard = checked
         }
 
         ToggleRow {
-            text: qsTr("Media")
+            text: qsTr("媒体")
             checked: Config.dashboard.showMedia
             onToggled: GlobalConfig.dashboard.showMedia = checked
         }
 
         ToggleRow {
-            text: qsTr("Performance")
+            text: qsTr("性能")
             checked: Config.dashboard.showPerformance
             onToggled: GlobalConfig.dashboard.showPerformance = checked
         }
 
         ToggleRow {
             last: true
-            text: qsTr("Weather")
+            text: qsTr("天气")
             checked: Config.dashboard.showWeather
             onToggled: GlobalConfig.dashboard.showWeather = checked
         }
 
         // Performance widgets
         SectionHeader {
-            text: qsTr("Performance widgets")
+            text: qsTr("性能组件")
         }
 
         ToggleRow {
             first: true
-            text: qsTr("Battery")
+            text: qsTr("电池")
             checked: Config.dashboard.performance.showBattery
             onToggled: GlobalConfig.dashboard.performance.showBattery = checked
         }
@@ -94,34 +94,34 @@ PageBase {
         }
 
         ToggleRow {
-            text: qsTr("Memory")
+            text: qsTr("内存")
             checked: Config.dashboard.performance.showMemory
             onToggled: GlobalConfig.dashboard.performance.showMemory = checked
         }
 
         ToggleRow {
-            text: qsTr("Storage")
+            text: qsTr("存储")
             checked: Config.dashboard.performance.showStorage
             onToggled: GlobalConfig.dashboard.performance.showStorage = checked
         }
 
         ToggleRow {
             last: true
-            text: qsTr("Network")
+            text: qsTr("网络")
             checked: Config.dashboard.performance.showNetwork
             onToggled: GlobalConfig.dashboard.performance.showNetwork = checked
         }
 
         // Behaviour
         SectionHeader {
-            text: qsTr("Behaviour")
+            text: qsTr("行为")
         }
 
         StepperRow {
             first: true
             last: true
-            label: qsTr("Drag threshold")
-            subtext: qsTr("Pixels dragged before the dashboard opens")
+            label: qsTr("拖动阈值")
+            subtext: qsTr("打开仪表盘前需要拖动的像素")
             value: Config.dashboard.dragThreshold
             from: 0
             to: 200
diff --git a/modules/nexus/pages/panels/LauncherPanel.qml b/modules/nexus/pages/panels/LauncherPanel.qml
index b78edcc29fdc3e075a5fefaec0dc212e36a0ca9c..002281b390b03a974c96d25d28ee9c9ada5a5b13 100644
--- a/modules/nexus/pages/panels/LauncherPanel.qml
+++ b/modules/nexus/pages/panels/LauncherPanel.qml
@@ -8,7 +8,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("Launcher")
+    title: qsTr("启动器")
     isSubPage: true
 
     ColumnLayout {
@@ -20,32 +20,32 @@ PageBase {
         // General
         SectionHeader {
             first: true
-            text: qsTr("General")
+            text: qsTr("通用")
         }
 
         ToggleRow {
             first: true
-            text: qsTr("Enabled")
+            text: qsTr("启用")
             checked: Config.launcher.enabled
             onToggled: GlobalConfig.launcher.enabled = checked
         }
 
         ToggleRow {
             last: true
-            text: qsTr("Show on hover")
-            subtext: qsTr("Reveal when the cursor reaches the screen edge")
+            text: qsTr("悬停显示")
+            subtext: qsTr("光标到达屏幕边缘时显示")
             checked: Config.launcher.showOnHover
             onToggled: GlobalConfig.launcher.showOnHover = checked
         }
 
         // Display
         SectionHeader {
-            text: qsTr("Display")
+            text: qsTr("显示")
         }
 
         StepperRow {
             first: true
-            label: qsTr("Max items shown")
+            label: qsTr("最大显示项目")
             value: Config.launcher.maxShown
             from: 1
             to: 20
@@ -54,7 +54,7 @@ PageBase {
         }
 
         StepperRow {
-            label: qsTr("Max wallpapers")
+            label: qsTr("最大壁纸数")
             value: Config.launcher.maxWallpapers
             from: 1
             to: 30
@@ -64,8 +64,8 @@ PageBase {
 
         StepperRow {
             last: true
-            label: qsTr("Drag threshold")
-            subtext: qsTr("Pixels dragged before the launcher opens")
+            label: qsTr("拖动阈值")
+            subtext: qsTr("打开启动器前需要拖动的像素")
             value: Config.launcher.dragThreshold
             from: 0
             to: 200
@@ -75,58 +75,58 @@ PageBase {
 
         // Behaviour
         SectionHeader {
-            text: qsTr("Behaviour")
+            text: qsTr("行为")
         }
 
         ToggleRow {
             first: true
-            text: qsTr("Vim keybinds")
-            subtext: qsTr("Navigate results with Ctrl+hjkl")
+            text: qsTr("Vim 键位")
+            subtext: qsTr("用 Ctrl+hjkl 导航结果")
             checked: GlobalConfig.launcher.vimKeybinds
             onToggled: GlobalConfig.launcher.vimKeybinds = checked
         }
 
         ToggleRow {
             last: true
-            text: qsTr("Enable dangerous actions")
-            subtext: qsTr("Allow actions that shut down or log out")
+            text: qsTr("启用危险操作")
+            subtext: qsTr("允许关机或注销操作")
             checked: GlobalConfig.launcher.enableDangerousActions
             onToggled: GlobalConfig.launcher.enableDangerousActions = checked
         }
 
         // Fuzzy search
         SectionHeader {
-            text: qsTr("Fuzzy search")
+            text: qsTr("模糊搜索")
         }
 
         ToggleRow {
             first: true
-            text: qsTr("Apps")
+            text: qsTr("应用")
             checked: GlobalConfig.launcher.useFuzzy.apps
             onToggled: GlobalConfig.launcher.useFuzzy.apps = checked
         }
 
         ToggleRow {
-            text: qsTr("Actions")
+            text: qsTr("操作")
             checked: GlobalConfig.launcher.useFuzzy.actions
             onToggled: GlobalConfig.launcher.useFuzzy.actions = checked
         }
 
         ToggleRow {
-            text: qsTr("Schemes")
+            text: qsTr("配色方案")
             checked: GlobalConfig.launcher.useFuzzy.schemes
             onToggled: GlobalConfig.launcher.useFuzzy.schemes = checked
         }
 
         ToggleRow {
-            text: qsTr("Variants")
+            text: qsTr("变体")
             checked: GlobalConfig.launcher.useFuzzy.variants
             onToggled: GlobalConfig.launcher.useFuzzy.variants = checked
         }
 
         ToggleRow {
             last: true
-            text: qsTr("Wallpapers")
+            text: qsTr("壁纸")
             checked: GlobalConfig.launcher.useFuzzy.wallpapers
             onToggled: GlobalConfig.launcher.useFuzzy.wallpapers = checked
         }
diff --git a/modules/nexus/pages/panels/SidebarPanel.qml b/modules/nexus/pages/panels/SidebarPanel.qml
index 8d6782248881cbb7e1ac529bf3e514835ecdbae4..eeb82d666a7c901cbf98a3233cdf9ad558501fcd 100644
--- a/modules/nexus/pages/panels/SidebarPanel.qml
+++ b/modules/nexus/pages/panels/SidebarPanel.qml
@@ -8,7 +8,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("Sidebar")
+    title: qsTr("侧边栏")
     isSubPage: true
 
     ColumnLayout {
@@ -19,20 +19,20 @@ PageBase {
 
         SectionHeader {
             first: true
-            text: qsTr("General")
+            text: qsTr("通用")
         }
 
         ToggleRow {
             first: true
-            text: qsTr("Enabled")
+            text: qsTr("启用")
             checked: Config.sidebar.enabled
             onToggled: GlobalConfig.sidebar.enabled = checked
         }
 
         StepperRow {
             last: true
-            label: qsTr("Drag threshold")
-            subtext: qsTr("Pixels dragged before the sidebar opens")
+            label: qsTr("拖动阈值")
+            subtext: qsTr("打开侧边栏前需要拖动的像素")
             value: Config.sidebar.dragThreshold
             from: 0
             to: 200
diff --git a/modules/nexus/pages/panels/TaskbarPanel.qml b/modules/nexus/pages/panels/TaskbarPanel.qml
index 97102f686b4561bf51b98441f4ce2877c57960fa..03aab90a4c8d12ace43c7bbbf58cd8d584fc79a6 100644
--- a/modules/nexus/pages/panels/TaskbarPanel.qml
+++ b/modules/nexus/pages/panels/TaskbarPanel.qml
@@ -7,7 +7,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("Taskbar")
+    title: qsTr("任务栏")
     isSubPage: true
 
     ColumnLayout {
@@ -19,28 +19,28 @@ PageBase {
         // Behaviour
         SectionHeader {
             first: true
-            text: qsTr("Behaviour")
+            text: qsTr("行为")
         }
 
         ToggleRow {
             first: true
-            text: qsTr("Persistent")
-            subtext: qsTr("Keep the bar visible at all times")
+            text: qsTr("常驻")
+            subtext: qsTr("始终显示任务栏")
             checked: Config.bar.persistent
             onToggled: GlobalConfig.bar.persistent = checked
         }
 
         ToggleRow {
-            text: qsTr("Show on hover")
-            subtext: qsTr("Reveal the bar when the cursor reaches the screen edge")
+            text: qsTr("悬停显示")
+            subtext: qsTr("光标到达屏幕边缘时显示任务栏")
             checked: Config.bar.showOnHover
             onToggled: GlobalConfig.bar.showOnHover = checked
         }
 
         StepperRow {
             last: true
-            label: qsTr("Drag threshold")
-            subtext: qsTr("Pixels dragged before the bar reveals")
+            label: qsTr("拖动阈值")
+            subtext: qsTr("显示任务栏前需要拖动的像素")
             value: Config.bar.dragThreshold
             from: 0
             to: 200
@@ -50,70 +50,70 @@ PageBase {
 
         // Components
         SectionHeader {
-            text: qsTr("Components")
+            text: qsTr("组件")
         }
 
         NavRow {
             first: true
             icon: "workspaces"
-            label: qsTr("Workspaces")
-            status: qsTr("Indicators, window icons")
+            label: qsTr("工作区")
+            status: qsTr("指示器、窗口图标")
             onClicked: root.nState.openSubPage(5)
         }
 
         NavRow {
             icon: "web_asset"
-            label: qsTr("Active window")
-            status: qsTr("Title display, popout")
+            label: qsTr("活动窗口")
+            status: qsTr("标题显示、弹窗")
             onClicked: root.nState.openSubPage(6)
         }
 
         NavRow {
             icon: "widgets"
-            label: qsTr("Tray")
-            status: qsTr("System tray icons")
+            label: qsTr("托盘")
+            status: qsTr("系统托盘图标")
             onClicked: root.nState.openSubPage(7)
         }
 
         NavRow {
             icon: "signal_cellular_alt"
-            label: qsTr("Status icons")
-            status: qsTr("Visible indicators")
+            label: qsTr("状态图标")
+            status: qsTr("可见指示器")
             onClicked: root.nState.openSubPage(8)
         }
 
         NavRow {
             last: true
             icon: "schedule"
-            label: qsTr("Clock")
-            status: qsTr("Date, icon, background")
+            label: qsTr("时钟")
+            status: qsTr("日期、图标、背景")
             onClicked: root.nState.openSubPage(9)
         }
 
         // Scroll actions
         SectionHeader {
-            text: qsTr("Scroll actions")
+            text: qsTr("滚轮操作")
         }
 
         ToggleRow {
             first: true
-            text: qsTr("Workspaces")
-            subtext: qsTr("Scroll over the workspace indicator to switch workspaces")
+            text: qsTr("工作区")
+            subtext: qsTr("在工作区指示器上滚动以切换工作区")
             checked: Config.bar.scrollActions.workspaces
             onToggled: GlobalConfig.bar.scrollActions.workspaces = checked
         }
 
         ToggleRow {
-            text: qsTr("Volume")
-            subtext: qsTr("Scroll on the top half of the bar to adjust volume")
+            text: qsTr("音量")
+            subtext: qsTr("在任务栏上半部分滚动以调节音量")
             checked: Config.bar.scrollActions.volume
             onToggled: GlobalConfig.bar.scrollActions.volume = checked
         }
 
         ToggleRow {
             last: true
-            text: qsTr("Brightness")
-            subtext: qsTr("Scroll on the bottom half of the bar to adjust brightness")
+            text: qsTr("亮度")
+            subtext: qsTr("在任务栏下半部分滚动以调节亮度")
             checked: Config.bar.scrollActions.brightness
             onToggled: GlobalConfig.bar.scrollActions.brightness = checked
         }
diff --git a/modules/nexus/pages/panels/taskbar/BarActiveWindow.qml b/modules/nexus/pages/panels/taskbar/BarActiveWindow.qml
index f51a1c4d5a0dc3feb05efe8e7ab82af32b3c3435..58d9caeb6b47c03d5cf46f32b6f7be9d92411143 100644
--- a/modules/nexus/pages/panels/taskbar/BarActiveWindow.qml
+++ b/modules/nexus/pages/panels/taskbar/BarActiveWindow.qml
@@ -7,7 +7,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("Active window")
+    title: qsTr("活动窗口")
     isSubPage: true
 
     ColumnLayout {
@@ -18,28 +18,28 @@ PageBase {
 
         ToggleRow {
             first: true
-            text: qsTr("Compact")
+            text: qsTr("紧凑")
             checked: Config.bar.activeWindow.compact
             onToggled: GlobalConfig.bar.activeWindow.compact = checked
         }
 
         ToggleRow {
-            text: qsTr("Inverted")
+            text: qsTr("反色")
             checked: Config.bar.activeWindow.inverted
             onToggled: GlobalConfig.bar.activeWindow.inverted = checked
         }
 
         ToggleRow {
-            text: qsTr("Show on hover")
-            subtext: qsTr("Only show the active window title while hovering")
+            text: qsTr("悬停显示")
+            subtext: qsTr("仅在悬停时显示活动窗口标题")
             checked: Config.bar.activeWindow.showOnHover
             onToggled: GlobalConfig.bar.activeWindow.showOnHover = checked
         }
 
         ToggleRow {
             last: true
-            text: qsTr("Popout on hover")
-            subtext: qsTr("Show a window details popout when hovering")
+            text: qsTr("悬停弹出")
+            subtext: qsTr("悬停时显示窗口详情弹窗")
             checked: Config.bar.popouts.activeWindow
             onToggled: GlobalConfig.bar.popouts.activeWindow = checked
         }
diff --git a/modules/nexus/pages/panels/taskbar/BarClock.qml b/modules/nexus/pages/panels/taskbar/BarClock.qml
index b2b6aef17654c6452ed5c34f5418578fe2ab3a13..f4990b6ded31e2eb378f37f1f14446aa96064365 100644
--- a/modules/nexus/pages/panels/taskbar/BarClock.qml
+++ b/modules/nexus/pages/panels/taskbar/BarClock.qml
@@ -7,7 +7,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("Clock")
+    title: qsTr("时钟")
     isSubPage: true
 
     ColumnLayout {
@@ -18,20 +18,20 @@ PageBase {
 
         ToggleRow {
             first: true
-            text: qsTr("Background")
+            text: qsTr("背景")
             checked: Config.bar.clock.background
             onToggled: GlobalConfig.bar.clock.background = checked
         }
 
         ToggleRow {
-            text: qsTr("Show date")
+            text: qsTr("显示日期")
             checked: Config.bar.clock.showDate
             onToggled: GlobalConfig.bar.clock.showDate = checked
         }
 
         ToggleRow {
             last: true
-            text: qsTr("Show icon")
+            text: qsTr("显示图标")
             checked: Config.bar.clock.showIcon
             onToggled: GlobalConfig.bar.clock.showIcon = checked
         }
diff --git a/modules/nexus/pages/panels/taskbar/BarStatusIcons.qml b/modules/nexus/pages/panels/taskbar/BarStatusIcons.qml
index bd0d91fe83ae003fca3a9d875d5d23c8cfa710ad..1acd5077cb900a31ba5a7e5d53dcab8a1c86448f 100644
--- a/modules/nexus/pages/panels/taskbar/BarStatusIcons.qml
+++ b/modules/nexus/pages/panels/taskbar/BarStatusIcons.qml
@@ -7,7 +7,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("Status icons")
+    title: qsTr("状态图标")
     isSubPage: true
 
     ColumnLayout {
@@ -19,69 +19,69 @@ PageBase {
         // Visible icons
         SectionHeader {
             first: true
-            text: qsTr("Visible icons")
+            text: qsTr("可见图标")
         }
 
         ToggleRow {
             first: true
-            text: qsTr("Speakers")
+            text: qsTr("扬声器")
             checked: Config.bar.status.showAudio
             onToggled: GlobalConfig.bar.status.showAudio = checked
         }
 
         ToggleRow {
-            text: qsTr("Microphone")
+            text: qsTr("麦克风")
             checked: Config.bar.status.showMicrophone
             onToggled: GlobalConfig.bar.status.showMicrophone = checked
         }
 
         ToggleRow {
-            text: qsTr("Keyboard layout")
+            text: qsTr("键盘布局")
             checked: Config.bar.status.showKbLayout
             onToggled: GlobalConfig.bar.status.showKbLayout = checked
         }
 
         ToggleRow {
-            text: qsTr("Network")
+            text: qsTr("网络")
             checked: Config.bar.status.showNetwork
             onToggled: GlobalConfig.bar.status.showNetwork = checked
         }
 
         ToggleRow {
-            text: qsTr("Wi-Fi")
+            text: qsTr("无线网络")
             checked: Config.bar.status.showWifi
             onToggled: GlobalConfig.bar.status.showWifi = checked
         }
 
         ToggleRow {
-            text: qsTr("Bluetooth")
+            text: qsTr("蓝牙")
             checked: Config.bar.status.showBluetooth
             onToggled: GlobalConfig.bar.status.showBluetooth = checked
         }
 
         ToggleRow {
-            text: qsTr("Battery")
+            text: qsTr("电池")
             checked: Config.bar.status.showBattery
             onToggled: GlobalConfig.bar.status.showBattery = checked
         }
 
         ToggleRow {
             last: true
-            text: qsTr("Caps lock")
+            text: qsTr("大写锁定")
             checked: Config.bar.status.showLockStatus
             onToggled: GlobalConfig.bar.status.showLockStatus = checked
         }
 
         // Behaviour
         SectionHeader {
-            text: qsTr("Behaviour")
+            text: qsTr("行为")
         }
 
         ToggleRow {
             first: true
             last: true
-            text: qsTr("Popout on hover")
-            subtext: qsTr("Show a details popout when hovering the status icons")
+            text: qsTr("悬停弹出")
+            subtext: qsTr("悬停状态图标时显示详情弹窗")
             checked: Config.bar.popouts.statusIcons
             onToggled: GlobalConfig.bar.popouts.statusIcons = checked
         }
diff --git a/modules/nexus/pages/panels/taskbar/BarTray.qml b/modules/nexus/pages/panels/taskbar/BarTray.qml
index a02597fdb684a5d004ca787df5a39c2e6da11814..390d64e3a8c7bd91dc8b647cdd45597ed986f9ae 100644
--- a/modules/nexus/pages/panels/taskbar/BarTray.qml
+++ b/modules/nexus/pages/panels/taskbar/BarTray.qml
@@ -7,7 +7,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("Tray")
+    title: qsTr("托盘")
     isSubPage: true
 
     ColumnLayout {
@@ -18,27 +18,27 @@ PageBase {
 
         ToggleRow {
             first: true
-            text: qsTr("Background")
+            text: qsTr("背景")
             checked: Config.bar.tray.background
             onToggled: GlobalConfig.bar.tray.background = checked
         }
 
         ToggleRow {
-            text: qsTr("Recolour icons")
+            text: qsTr("重绘图标颜色")
             checked: Config.bar.tray.recolour
             onToggled: GlobalConfig.bar.tray.recolour = checked
         }
 
         ToggleRow {
-            text: qsTr("Compact")
+            text: qsTr("紧凑")
             checked: Config.bar.tray.compact
             onToggled: GlobalConfig.bar.tray.compact = checked
         }
 
         ToggleRow {
             last: true
-            text: qsTr("Popout on hover")
-            subtext: qsTr("Show the tray menu popout when hovering")
+            text: qsTr("悬停弹出")
+            subtext: qsTr("悬停时显示托盘菜单")
             checked: Config.bar.popouts.tray
             onToggled: GlobalConfig.bar.popouts.tray = checked
         }
diff --git a/modules/nexus/pages/panels/taskbar/BarWorkspaces.qml b/modules/nexus/pages/panels/taskbar/BarWorkspaces.qml
index 814dc862650fa98f1a745b987fc3993af19a2ee9..ce48e64eb558442f5c5c7f9ce33387605eb51059 100644
--- a/modules/nexus/pages/panels/taskbar/BarWorkspaces.qml
+++ b/modules/nexus/pages/panels/taskbar/BarWorkspaces.qml
@@ -7,7 +7,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("Workspaces")
+    title: qsTr("工作区")
     isSubPage: true
 
     ColumnLayout {
@@ -18,8 +18,8 @@ PageBase {
 
         StepperRow {
             first: true
-            label: qsTr("Shown")
-            subtext: qsTr("Number of workspaces displayed")
+            label: qsTr("显示数量")
+            subtext: qsTr("显示的工作区数量")
             value: Config.bar.workspaces.shown
             from: 1
             to: 20
@@ -28,38 +28,38 @@ PageBase {
         }
 
         ToggleRow {
-            text: qsTr("Active indicator")
+            text: qsTr("活动指示器")
             checked: Config.bar.workspaces.activeIndicator
             onToggled: GlobalConfig.bar.workspaces.activeIndicator = checked
         }
 
         ToggleRow {
-            text: qsTr("Active trail")
+            text: qsTr("活动轨迹")
             checked: Config.bar.workspaces.activeTrail
             onToggled: GlobalConfig.bar.workspaces.activeTrail = checked
         }
 
         ToggleRow {
-            text: qsTr("Occupied background")
+            text: qsTr("已占用背景")
             checked: Config.bar.workspaces.occupiedBg
             onToggled: GlobalConfig.bar.workspaces.occupiedBg = checked
         }
 
         ToggleRow {
-            text: qsTr("Show windows")
-            subtext: qsTr("Show icons of open windows on each workspace")
+            text: qsTr("显示窗口")
+            subtext: qsTr("在每个工作区显示已打开窗口图标")
             checked: Config.bar.workspaces.showWindows
             onToggled: GlobalConfig.bar.workspaces.showWindows = checked
         }
 
         ToggleRow {
-            text: qsTr("Windows on special workspaces")
+            text: qsTr("特殊工作区中的窗口")
             checked: Config.bar.workspaces.showWindowsOnSpecialWorkspaces
             onToggled: GlobalConfig.bar.workspaces.showWindowsOnSpecialWorkspaces = checked
         }
 
         StepperRow {
-            label: qsTr("Max window icons")
+            label: qsTr("最大窗口图标数")
             value: Config.bar.workspaces.maxWindowIcons
             from: 0
             to: 20
@@ -69,8 +69,8 @@ PageBase {
 
         ToggleRow {
             last: true
-            text: qsTr("Per-monitor workspaces")
-            subtext: qsTr("Show each monitor's workspaces independently")
+            text: qsTr("按显示器显示工作区")
+            subtext: qsTr("每个显示器独立显示工作区")
             checked: GlobalConfig.bar.workspaces.perMonitorWorkspaces
             onToggled: GlobalConfig.bar.workspaces.perMonitorWorkspaces = checked
         }
diff --git a/modules/nexus/pages/services/NotificationsPage.qml b/modules/nexus/pages/services/NotificationsPage.qml
index 78a03b8c53a941c9a2715a787fcb8087602db4ef..ead2e2240544364bd06c037ac7d798b991b772e2 100644
--- a/modules/nexus/pages/services/NotificationsPage.qml
+++ b/modules/nexus/pages/services/NotificationsPage.qml
@@ -10,11 +10,11 @@ PageBase {
     // Notification fullscreen visibility, mapped to GlobalConfig.notifs.fullscreen
     readonly property list<MenuItem> notifFullscreenItems: [
         MenuItem {
-            text: qsTr("Off")
+            text: qsTr("关")
             icon: "notifications_off"
         },
         MenuItem {
-            text: qsTr("On")
+            text: qsTr("开")
             icon: "notifications"
         }
     ]
@@ -23,21 +23,21 @@ PageBase {
     // Toast fullscreen visibility, mapped to GlobalConfig.utilities.toasts.fullscreen
     readonly property list<MenuItem> toastFullscreenItems: [
         MenuItem {
-            text: qsTr("Off")
+            text: qsTr("关")
             icon: "notifications_off"
         },
         MenuItem {
-            text: qsTr("Important")
+            text: qsTr("重要")
             icon: "priority_high"
         },
         MenuItem {
-            text: qsTr("On")
+            text: qsTr("开")
             icon: "notifications"
         }
     ]
     readonly property list<string> toastFullscreenValues: ["off", "important", "all"]
 
-    title: qsTr("Notifications")
+    title: qsTr("通知")
     isSubPage: true
 
     ColumnLayout {
@@ -49,35 +49,35 @@ PageBase {
         // Notifications
         SectionHeader {
             first: true
-            text: qsTr("Notifications")
+            text: qsTr("通知")
         }
 
         SelectRow {
             first: true
-            label: qsTr("Show in fullscreen")
-            subtext: qsTr("Whether notifications appear over fullscreen apps")
+            label: qsTr("全屏时显示")
+            subtext: qsTr("通知是否显示在全屏应用上方")
             menuItems: root.notifFullscreenItems
             active: root.notifFullscreenItems[Math.max(0, root.notifFullscreenValues.indexOf(GlobalConfig.notifs.fullscreen))]
             onSelected: item => GlobalConfig.notifs.fullscreen = root.notifFullscreenValues[root.notifFullscreenItems.indexOf(item)]
         }
 
         ToggleRow {
-            text: qsTr("Expire automatically")
-            subtext: qsTr("Dismiss notifications after their timeout")
+            text: qsTr("自动过期")
+            subtext: qsTr("超时后关闭通知")
             checked: GlobalConfig.notifs.expire
             onToggled: GlobalConfig.notifs.expire = checked
         }
 
         ToggleRow {
-            text: qsTr("Open expanded")
-            subtext: qsTr("Show notifications expanded by default")
+            text: qsTr("默认展开")
+            subtext: qsTr("默认展开显示通知")
             checked: GlobalConfig.notifs.openExpanded
             onToggled: GlobalConfig.notifs.openExpanded = checked
         }
 
         StepperRow {
-            label: qsTr("Default timeout")
-            subtext: qsTr("Time before a notification dismisses (ms)")
+            label: qsTr("默认超时")
+            subtext: qsTr("通知关闭前的时间（毫秒）")
             value: GlobalConfig.notifs.defaultExpireTimeout
             from: 1000
             to: 60000
@@ -87,8 +87,8 @@ PageBase {
 
         StepperRow {
             last: true
-            label: qsTr("Group preview count")
-            subtext: qsTr("Notifications shown per group before collapsing")
+            label: qsTr("分组预览数量")
+            subtext: qsTr("折叠前每组显示的通知数")
             value: GlobalConfig.notifs.groupPreviewNum
             from: 1
             to: 10
@@ -98,13 +98,13 @@ PageBase {
 
         // Toasts
         SectionHeader {
-            text: qsTr("Toasts")
+            text: qsTr("提示")
         }
 
         SelectRow {
             first: true
-            label: qsTr("Show in fullscreen")
-            subtext: qsTr("Whether toasts appear over fullscreen apps")
+            label: qsTr("全屏时显示")
+            subtext: qsTr("提示是否显示在全屏应用上方")
             menuItems: root.toastFullscreenItems
             active: root.toastFullscreenItems[Math.max(0, root.toastFullscreenValues.indexOf(GlobalConfig.utilities.toasts.fullscreen))]
             onSelected: item => GlobalConfig.utilities.toasts.fullscreen = root.toastFullscreenValues[root.toastFullscreenItems.indexOf(item)]
@@ -112,8 +112,8 @@ PageBase {
 
         StepperRow {
             last: true
-            label: qsTr("Visible toasts")
-            subtext: qsTr("Maximum number of toasts shown at once")
+            label: qsTr("可见提示数")
+            subtext: qsTr("同时显示的最大提示数")
             value: GlobalConfig.utilities.maxToasts
             from: 1
             to: 10
@@ -123,67 +123,67 @@ PageBase {
 
         // Toast events
         SectionHeader {
-            text: qsTr("Toast events")
+            text: qsTr("提示事件")
         }
 
         ToggleRow {
             first: true
-            text: qsTr("Charging changes")
+            text: qsTr("充电状态变化")
             checked: GlobalConfig.utilities.toasts.chargingChanged
             onToggled: GlobalConfig.utilities.toasts.chargingChanged = checked
         }
 
         ToggleRow {
-            text: qsTr("Game mode changes")
+            text: qsTr("游戏模式变化")
             checked: GlobalConfig.utilities.toasts.gameModeChanged
             onToggled: GlobalConfig.utilities.toasts.gameModeChanged = checked
         }
 
         ToggleRow {
-            text: qsTr("Do not disturb changes")
+            text: qsTr("勿扰模式变化")
             checked: GlobalConfig.utilities.toasts.dndChanged
             onToggled: GlobalConfig.utilities.toasts.dndChanged = checked
         }
 
         ToggleRow {
-            text: qsTr("Audio output changes")
+            text: qsTr("音频输出变化")
             checked: GlobalConfig.utilities.toasts.audioOutputChanged
             onToggled: GlobalConfig.utilities.toasts.audioOutputChanged = checked
         }
 
         ToggleRow {
-            text: qsTr("Audio input changes")
+            text: qsTr("音频输入变化")
             checked: GlobalConfig.utilities.toasts.audioInputChanged
             onToggled: GlobalConfig.utilities.toasts.audioInputChanged = checked
         }
 
         ToggleRow {
-            text: qsTr("Caps lock changes")
+            text: qsTr("大写锁定变化")
             checked: GlobalConfig.utilities.toasts.capsLockChanged
             onToggled: GlobalConfig.utilities.toasts.capsLockChanged = checked
         }
 
         ToggleRow {
-            text: qsTr("Num lock changes")
+            text: qsTr("数字锁定变化")
             checked: GlobalConfig.utilities.toasts.numLockChanged
             onToggled: GlobalConfig.utilities.toasts.numLockChanged = checked
         }
 
         ToggleRow {
-            text: qsTr("Keyboard layout changes")
+            text: qsTr("键盘布局变化")
             checked: GlobalConfig.utilities.toasts.kbLayoutChanged
             onToggled: GlobalConfig.utilities.toasts.kbLayoutChanged = checked
         }
 
         ToggleRow {
-            text: qsTr("VPN changes")
+            text: qsTr("VPN 变化")
             checked: GlobalConfig.utilities.toasts.vpnChanged
             onToggled: GlobalConfig.utilities.toasts.vpnChanged = checked
         }
 
         ToggleRow {
             last: true
-            text: qsTr("Now playing")
+            text: qsTr("正在播放")
             checked: GlobalConfig.utilities.toasts.nowPlaying
             onToggled: GlobalConfig.utilities.toasts.nowPlaying = checked
         }
diff --git a/modules/nexus/pages/wallandstyle/ColourSelect.qml b/modules/nexus/pages/wallandstyle/ColourSelect.qml
index fd730ea5138b1e164cbcaa0549b9bbf39f2b6dc0..3e0c1b29b33660d674207445cd1aac4beedfd51b 100644
--- a/modules/nexus/pages/wallandstyle/ColourSelect.qml
+++ b/modules/nexus/pages/wallandstyle/ColourSelect.qml
@@ -8,7 +8,7 @@ import qs.modules.nexus.common
 PageBase {
     id: root
 
-    title: qsTr("Colours")
+    title: qsTr("颜色")
     isSubPage: true
 
     Item {
@@ -31,14 +31,14 @@ PageBase {
 
             StyledText {
                 Layout.alignment: Qt.AlignHCenter
-                text: qsTr("Page under construction")
+                text: qsTr("页面建设中")
                 color: Colours.palette.m3outlineVariant
                 font: Tokens.font.title.large
             }
 
             StyledText {
                 Layout.alignment: Qt.AlignHCenter
-                text: qsTr("This page will be available in a future update.")
+                text: qsTr("此页面将在未来更新中可用。")
                 color: Colours.palette.m3outlineVariant
                 font: Tokens.font.body.large
             }
diff --git a/modules/sidebar/Notif.qml b/modules/sidebar/Notif.qml
index 641c3348fc6c2fb372f16cb034db0da39c3a68b0..3f7612b7d5bf9eb84009a35de1eb3db93e630ea6 100644
--- a/modules/sidebar/Notif.qml
+++ b/modules/sidebar/Notif.qml
@@ -135,7 +135,7 @@ StyledRect {
 
             Layout.fillWidth: true
             textFormat: Text.MarkdownText
-            text: String(root.modelData?.body ?? "").replace(/(.)\n(?!\n)/g, "$1\n\n") || qsTr("No body here! :/")
+            text: String(root.modelData?.body ?? "").replace(/(.)\n(?!\n)/g, "$1\n\n") || qsTr("没有通知内容")
             color: root.modelData?.urgency === "critical" ? Colours.palette.m3secondary : Colours.palette.m3outline
             wrapMode: Text.WordWrap
 
diff --git a/modules/sidebar/NotifDock.qml b/modules/sidebar/NotifDock.qml
index 10014a436bad11aaad38313ace55c743a57ecf59..0bc9a70e12cb74c736ab9bc18a48460af6e5f9e1 100644
--- a/modules/sidebar/NotifDock.qml
+++ b/modules/sidebar/NotifDock.qml
@@ -65,7 +65,7 @@ Item {
             anchors.right: parent.right
             anchors.leftMargin: Tokens.spacing.extraSmall
 
-            text: root.notifCount > 0 ? qsTr("notification%1").arg(root.notifCount === 1 ? "" : "s") : qsTr("Notifications")
+            text: root.notifCount > 0 ? qsTr("条通知") : qsTr("通知")
             color: Colours.palette.m3outline
             font: Tokens.font.label.large
             elide: Text.ElideRight
@@ -108,7 +108,7 @@ Item {
 
                 StyledText {
                     Layout.alignment: Qt.AlignHCenter
-                    text: qsTr("All up to date!")
+                    text: qsTr("没有新通知")
                     color: Colours.palette.m3outlineVariant
                     font: Tokens.font.headline.builders.small.width(90).build()
                 }
diff --git a/modules/utilities/RecordingDeleteModal.qml b/modules/utilities/RecordingDeleteModal.qml
index 2e462b466bade20c33b80811034591a87d2a1090..e2203251fb2655895c19a20ef3c0def977be0ba8 100644
--- a/modules/utilities/RecordingDeleteModal.qml
+++ b/modules/utilities/RecordingDeleteModal.qml
@@ -167,13 +167,13 @@ Loader {
                 spacing: Tokens.spacing.medium
 
                 StyledText {
-                    text: qsTr("Delete recording?")
+                    text: qsTr("删除录制？")
                     font: Tokens.font.body.large
                 }
 
                 StyledText {
                     Layout.fillWidth: true
-                    text: qsTr("Recording '%1' will be permanently deleted.").arg(deleteConfirmation.path)
+                    text: qsTr("录制“%1”将被永久删除。").arg(deleteConfirmation.path)
                     color: Colours.palette.m3onSurfaceVariant
                     font: Tokens.font.body.small
                     wrapMode: Text.WrapAtWordBoundaryOrAnywhere
@@ -185,13 +185,13 @@ Loader {
                     spacing: Tokens.spacing.medium
 
                     TextButton {
-                        text: qsTr("Cancel")
+                        text: qsTr("取消")
                         type: TextButton.Text
                         onClicked: root.props.recordingConfirmDelete = ""
                     }
 
                     TextButton {
-                        text: qsTr("Delete")
+                        text: qsTr("删除")
                         type: TextButton.Text
                         onClicked: {
                             CUtils.deleteFile(Qt.resolvedUrl(root.props.recordingConfirmDelete));
diff --git a/modules/utilities/cards/RecordingList.qml b/modules/utilities/cards/RecordingList.qml
index a890ac74898164efa5be2676dba05dd0a6898816..97a7801f65e7feaa9bb3a0cb93553ed32b938595 100644
--- a/modules/utilities/cards/RecordingList.qml
+++ b/modules/utilities/cards/RecordingList.qml
@@ -38,7 +38,7 @@ ColumnLayout {
             StyledText {
                 Layout.alignment: Qt.AlignVCenter
                 Layout.fillWidth: true
-                text: qsTr("Recordings")
+                text: qsTr("录制文件")
                 font: Tokens.font.body.medium
             }
 
@@ -92,7 +92,7 @@ ColumnLayout {
                         return time;
                     const date = new Date(...matches.slice(1));
                     date.setMonth(date.getMonth() - 1); // Woe (months start from 0)
-                    return qsTr("Recording at %1").arg(Qt.formatDateTime(date, Qt.locale()));
+                    return qsTr("录制于 %1").arg(Qt.formatDateTime(date, Qt.locale()));
                 }
                 color: Colours.palette.m3onSurfaceVariant
                 elide: Text.ElideRight
@@ -218,7 +218,7 @@ ColumnLayout {
                     }
 
                     StyledText {
-                        text: qsTr("No recordings found")
+                        text: qsTr("未找到录制文件")
                         color: Colours.palette.m3outline
                     }
                 }
diff --git a/modules/windowinfo/Buttons.qml b/modules/windowinfo/Buttons.qml
index 7a4fb743176ad099f06edff54f0e4dc1c69421f1..be0362c22abf7ccfd09e13cc448ccdc429aafeff 100644
--- a/modules/windowinfo/Buttons.qml
+++ b/modules/windowinfo/Buttons.qml
@@ -24,7 +24,7 @@ ColumnLayout {
 
         StyledText {
             Layout.fillWidth: true
-            text: qsTr("Move to workspace")
+            text: qsTr("移动到工作区")
             elide: Text.ElideRight
         }
 
@@ -117,7 +117,7 @@ ColumnLayout {
         Button {
             color: Colours.palette.m3secondaryContainer
             onColor: Colours.palette.m3onSecondaryContainer
-            text: root.client?.lastIpcObject.floating ? qsTr("Tile") : qsTr("Float")
+            text: root.client?.lastIpcObject.floating ? qsTr("平铺") : qsTr("浮动")
             onClicked: Hypr.dispatch(Hypr.usingLua ? `hl.dsp.window.float({ window = "address:0x${root.client?.address}" })` : `togglefloating address:0x${root.client?.address}`)
         }
 
@@ -131,7 +131,7 @@ ColumnLayout {
             sourceComponent: Button {
                 color: Colours.palette.m3secondaryContainer
                 onColor: Colours.palette.m3onSecondaryContainer
-                text: root.client?.lastIpcObject.pinned ? qsTr("Unpin") : qsTr("Pin")
+                text: root.client?.lastIpcObject.pinned ? qsTr("取消置顶") : qsTr("置顶")
                 onClicked: Hypr.dispatch(Hypr.usingLua ? `hl.dsp.window.pin({ window = "address:0x${root.client?.address}" })` : `pin address:0x${root.client?.address}`)
             }
         }
@@ -139,7 +139,7 @@ ColumnLayout {
         Button {
             color: Colours.palette.m3errorContainer
             onColor: Colours.palette.m3onErrorContainer
-            text: qsTr("Kill")
+            text: qsTr("关闭")
             onClicked: Hypr.dispatch(Hypr.usingLua ? `hl.dsp.window.kill({ window = "address:0x${root.client?.address}" })` : `killwindow address:0x${root.client?.address}`)
         }
     }
diff --git a/modules/windowinfo/Details.qml b/modules/windowinfo/Details.qml
index fc96d414e2c91422f6b6a45d0d983bc20f50bd13..a3a467a7e5187abc5a2d7646934f89383f146ed6 100644
--- a/modules/windowinfo/Details.qml
+++ b/modules/windowinfo/Details.qml
@@ -16,14 +16,14 @@ ColumnLayout {
     Label {
         Layout.topMargin: Tokens.padding.extraLargeIncreased
 
-        text: root.client?.title ?? qsTr("No active client")
+        text: root.client?.title ?? qsTr("没有活动窗口")
         wrapMode: Text.WrapAtWordBoundaryOrAnywhere
 
         font: Tokens.font.body.builders.large.weight(Font.Medium).build()
     }
 
     Label {
-        text: root.client?.lastIpcObject.class ?? qsTr("No active client")
+        text: root.client?.lastIpcObject.class ?? qsTr("没有活动窗口")
         color: Colours.palette.m3tertiary
 
         font: Tokens.font.body.large
@@ -42,24 +42,24 @@ ColumnLayout {
 
     Detail {
         icon: "location_on"
-        text: qsTr("Address: %1").arg(`0x${root.client?.address}` ?? "unknown")
+        text: qsTr("地址：%1").arg(`0x${root.client?.address}` ?? "unknown")
         color: Colours.palette.m3primary
     }
 
     Detail {
         icon: "location_searching"
-        text: qsTr("Position: %1, %2").arg(root.client?.lastIpcObject.at[0] ?? -1).arg(root.client?.lastIpcObject.at[1] ?? -1)
+        text: qsTr("位置：%1，%2").arg(root.client?.lastIpcObject.at[0] ?? -1).arg(root.client?.lastIpcObject.at[1] ?? -1)
     }
 
     Detail {
         icon: "resize"
-        text: qsTr("Size: %1 x %2").arg(root.client?.lastIpcObject.size[0] ?? -1).arg(root.client?.lastIpcObject.size[1] ?? -1)
+        text: qsTr("大小：%1 × %2").arg(root.client?.lastIpcObject.size[0] ?? -1).arg(root.client?.lastIpcObject.size[1] ?? -1)
         color: Colours.palette.m3tertiary
     }
 
     Detail {
         icon: "workspaces"
-        text: qsTr("Workspace: %1 (%2)").arg(root.client?.workspace.name ?? -1).arg(root.client?.workspace.id ?? -1)
+        text: qsTr("工作区：%1（%2）").arg(root.client?.workspace.name ?? -1).arg(root.client?.workspace.id ?? -1)
         color: Colours.palette.m3secondary
     }
 
@@ -68,42 +68,42 @@ ColumnLayout {
         text: {
             const mon = root.client?.monitor;
             if (mon)
-                return qsTr("Monitor: %1 (%2) at %3, %4").arg(mon.name).arg(mon.id).arg(mon.x).arg(mon.y);
-            return qsTr("Monitor: unknown");
+                return qsTr("显示器：%1（%2），位置 %3，%4").arg(mon.name).arg(mon.id).arg(mon.x).arg(mon.y);
+            return qsTr("显示器：未知");
         }
     }
 
     Detail {
         icon: "page_header"
-        text: qsTr("Initial title: %1").arg(root.client?.lastIpcObject.initialTitle ?? "unknown")
+        text: qsTr("初始标题：%1").arg(root.client?.lastIpcObject.initialTitle ?? "unknown")
         color: Colours.palette.m3tertiary
     }
 
     Detail {
         icon: "category"
-        text: qsTr("Initial class: %1").arg(root.client?.lastIpcObject.initialClass ?? "unknown")
+        text: qsTr("初始类名：%1").arg(root.client?.lastIpcObject.initialClass ?? "unknown")
     }
 
     Detail {
         icon: "account_tree"
-        text: qsTr("Process id: %1").arg(String(root.client?.lastIpcObject.pid ?? -1))
+        text: qsTr("进程 ID：%1").arg(String(root.client?.lastIpcObject.pid ?? -1))
         color: Colours.palette.m3primary
     }
 
     Detail {
         icon: "picture_in_picture_center"
-        text: qsTr("Floating: %1").arg(root.client?.lastIpcObject.floating ? "yes" : "no")
+        text: qsTr("浮动：%1").arg(root.client?.lastIpcObject.floating ? qsTr("是") : qsTr("否"))
         color: Colours.palette.m3secondary
     }
 
     Detail {
         icon: "gradient"
-        text: qsTr("Xwayland: %1").arg(root.client?.lastIpcObject.xwayland ? "yes" : "no")
+        text: qsTr("XWayland：%1").arg(root.client?.lastIpcObject.xwayland ? qsTr("是") : qsTr("否"))
     }
 
     Detail {
         icon: "keep"
-        text: qsTr("Pinned: %1").arg(root.client?.lastIpcObject.pinned ? "yes" : "no")
+        text: qsTr("置顶：%1").arg(root.client?.lastIpcObject.pinned ? qsTr("是") : qsTr("否"))
         color: Colours.palette.m3secondary
     }
 
@@ -112,8 +112,8 @@ ColumnLayout {
         text: {
             const fs = root.client?.lastIpcObject.fullscreen;
             if (fs)
-                return qsTr("Fullscreen state: %1").arg(fs == 0 ? "off" : fs == 1 ? "maximised" : "on");
-            return qsTr("Fullscreen state: unknown");
+                return qsTr("全屏状态：%1").arg(fs == 0 ? qsTr("关闭") : fs == 1 ? qsTr("最大化") : qsTr("开启"));
+            return qsTr("全屏状态：未知");
         }
         color: Colours.palette.m3tertiary
     }
diff --git a/modules/windowinfo/Preview.qml b/modules/windowinfo/Preview.qml
index 4ff03480a964e56c4f437a02f93d8a6379e9da30..114b41c1251c022d56f741cb2ba99c05c7d9d2d4 100644
--- a/modules/windowinfo/Preview.qml
+++ b/modules/windowinfo/Preview.qml
@@ -49,14 +49,14 @@ Item {
 
                 StyledText {
                     Layout.alignment: Qt.AlignHCenter
-                    text: qsTr("No active client")
+                    text: qsTr("没有活动窗口")
                     color: Colours.palette.m3outline
                     font: Tokens.font.body.builders.large.size(28).weight(Font.Medium).build()
                 }
 
                 StyledText {
                     Layout.alignment: Qt.AlignHCenter
-                    text: qsTr("Try switching to a window")
+                    text: qsTr("请切换到一个窗口")
                     color: Colours.palette.m3outline
                     font: Tokens.font.body.large
                 }
@@ -87,7 +87,7 @@ Item {
         text: {
             const client = root.client;
             if (!client)
-                return qsTr("No active client");
+                return qsTr("没有活动窗口");
 
             const mon = client.monitor;
             return qsTr("%1 on monitor %2 at %3, %4").arg(client.title).arg(mon.name).arg(client.lastIpcObject.at[0]).arg(client.lastIpcObject.at[1]);
diff --git a/services/Audio.qml b/services/Audio.qml
index 6268b92edbd5a8b5ff68215d303678d429f76533..78962a2a5f23e6d5d5d8bf401410986a77660e2d 100644
--- a/services/Audio.qml
+++ b/services/Audio.qml
@@ -100,19 +100,19 @@ Singleton {
 
     function getStreamName(stream: PwNode): string {
         if (!stream)
-            return qsTr("Unknown");
+            return qsTr("未知");
         // Try application name first, then description, then name
-        return stream.properties["application.name"] || stream.description || stream.name || qsTr("Unknown Application");
+        return stream.properties["application.name"] || stream.description || stream.name || qsTr("未知应用");
     }
 
     onSinkChanged: {
         if (!sink?.ready)
             return;
 
-        const newSinkName = sink.description || sink.name || qsTr("Unknown Device");
+        const newSinkName = sink.description || sink.name || qsTr("未知设备");
 
         if (previousSinkName && previousSinkName !== newSinkName && GlobalConfig.utilities.toasts.audioOutputChanged)
-            Toaster.toast(qsTr("Audio output changed"), qsTr("Now using: %1").arg(newSinkName), "volume_up");
+            Toaster.toast(qsTr("音频输出已更改"), qsTr("当前使用：%1").arg(newSinkName), "volume_up");
 
         previousSinkName = newSinkName;
     }
@@ -121,17 +121,17 @@ Singleton {
         if (!source?.ready)
             return;
 
-        const newSourceName = source.description || source.name || qsTr("Unknown Device");
+        const newSourceName = source.description || source.name || qsTr("未知设备");
 
         if (previousSourceName && previousSourceName !== newSourceName && GlobalConfig.utilities.toasts.audioInputChanged)
-            Toaster.toast(qsTr("Audio input changed"), qsTr("Now using: %1").arg(newSourceName), "mic");
+            Toaster.toast(qsTr("音频输入已更改"), qsTr("当前使用：%1").arg(newSourceName), "mic");
 
         previousSourceName = newSourceName;
     }
 
     Component.onCompleted: {
-        previousSinkName = sink?.description || sink?.name || qsTr("Unknown Device");
-        previousSourceName = source?.description || source?.name || qsTr("Unknown Device");
+        previousSinkName = sink?.description || sink?.name || qsTr("未知设备");
+        previousSourceName = source?.description || source?.name || qsTr("未知设备");
     }
 
     Connections {
diff --git a/services/Brightness.qml b/services/Brightness.qml
index ac34cbc1c762532c3d829e2d01c80e67c7d78781..9e891d1f26ad4e683d99ecb9b88efa6341b24bb6 100644
--- a/services/Brightness.qml
+++ b/services/Brightness.qml
@@ -96,7 +96,7 @@ Singleton {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "brightnessUp"
-        description: "Increase brightness"
+        description: "提高亮度"
         onPressed: root.increaseBrightness()
     }
 
@@ -104,7 +104,7 @@ Singleton {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "brightnessDown"
-        description: "Decrease brightness"
+        description: "降低亮度"
         onPressed: root.decreaseBrightness()
     }
 
diff --git a/services/GameMode.qml b/services/GameMode.qml
index 6dfc791c5289e1a3ff8fb67fb3a359649d29fc4d..949417ccc3307fb46a7070fc8dbad31a8a2ae019 100644
--- a/services/GameMode.qml
+++ b/services/GameMode.qml
@@ -29,11 +29,11 @@ Singleton {
         if (enabled) {
             setDynamicConfs();
             if (GlobalConfig.utilities.toasts.gameModeChanged)
-                Toaster.toast(qsTr("Game mode enabled"), qsTr("Disabled Hyprland animations, blur, gaps and shadows"), "gamepad");
+                Toaster.toast(qsTr("游戏模式已启用"), qsTr("已关闭 Hyprland 动画、模糊、间距和阴影"), "gamepad");
         } else {
             Hypr.extras.message("reload");
             if (GlobalConfig.utilities.toasts.gameModeChanged)
-                Toaster.toast(qsTr("Game mode disabled"), qsTr("Hyprland settings restored"), "gamepad");
+                Toaster.toast(qsTr("游戏模式已关闭"), qsTr("Hyprland 设置已恢复"), "gamepad");
         }
     }
 
diff --git a/services/Hypr.qml b/services/Hypr.qml
index bcb3ec00060566949f426ba18f22cc88cb17d8f8..15122e83baa8d78bac3879fa69c666516659abd9 100644
--- a/services/Hypr.qml
+++ b/services/Hypr.qml
@@ -29,7 +29,7 @@ Singleton {
     readonly property bool capsLock: keyboard?.capsLock ?? false
     readonly property bool numLock: keyboard?.numLock ?? false
     readonly property string defaultKbLayout: keyboard?.layout.split(",")[0] ?? "??"
-    readonly property string kbLayoutFull: keyboard?.activeKeymap ?? "Unknown"
+    readonly property string kbLayoutFull: keyboard?.activeKeymap ?? "未知"
     readonly property string kbLayout: kbMap.get(kbLayoutFull) ?? "??"
     readonly property var kbMap: new Map()
 
@@ -217,7 +217,7 @@ Singleton {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "refreshDevices"
-        description: "Reload devices"
+        description: "重新加载设备"
         onPressed: extras.refreshDevices()
         onReleased: extras.refreshDevices()
     }
diff --git a/services/NotifData.qml b/services/NotifData.qml
index 8e980f189d298c54d4dd37e4e6cf4f4edd6f6166..4bff8f6a600cb46f5d2bfbdd49f665fce49e78da 100644
--- a/services/NotifData.qml
+++ b/services/NotifData.qml
@@ -16,7 +16,7 @@ QtObject {
     property var locks: new Set()
 
     property date time: new Date()
-    property string timeStr: qsTr("now")
+    property string timeStr: qsTr("刚刚")
 
     readonly property Timer timeStrTimer: Timer {
         running: !notif.closed
@@ -173,7 +173,7 @@ QtObject {
         const m = Math.floor(diff / 60000);
 
         if (m < 1) {
-            timeStr = qsTr("now");
+            timeStr = qsTr("刚刚");
             timeStrTimer.interval = 5000;
         } else {
             const h = Math.floor(m / 60);
diff --git a/services/Notifs.qml b/services/Notifs.qml
index 87645c4a5b403a7ec30aeca5a505b579d9061c37..e76c97e5d863df7c7ad6780c1e849c4cd457b0fe 100644
--- a/services/Notifs.qml
+++ b/services/Notifs.qml
@@ -42,9 +42,9 @@ Singleton {
             return;
 
         if (dnd)
-            Toaster.toast(qsTr("Do not disturb enabled"), qsTr("Popup notifications are now disabled"), "do_not_disturb_on");
+            Toaster.toast(qsTr("免打扰已启用"), qsTr("弹出通知现已关闭"), "do_not_disturb_on");
         else
-            Toaster.toast(qsTr("Do not disturb disabled"), qsTr("Popup notifications are now enabled"), "do_not_disturb_off");
+            Toaster.toast(qsTr("免打扰已关闭"), qsTr("弹出通知现已启用"), "do_not_disturb_off");
     }
 
     onListChanged: {
@@ -134,7 +134,7 @@ Singleton {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "clearNotifs"
-        description: "Clear all notifications"
+        description: "清除所有通知"
         onPressed: {
             for (const notif of root.list.slice())
                 notif.close();
diff --git a/services/Players.qml b/services/Players.qml
index 59f962141e499695b54749eb646b15fafd7354a6..750440a0389748d036e681b3e2d0832fe9ea1264 100644
--- a/services/Players.qml
+++ b/services/Players.qml
@@ -43,7 +43,7 @@ Singleton {
                 return;
             }
             if (root.active.trackArtist != "" && root.active.trackTitle != "") {
-                Toaster.toast(qsTr("Now Playing"), qsTr("%1 - %2").arg(root.active.trackArtist).arg(root.active.trackTitle), "music_note");
+                Toaster.toast(qsTr("正在播放"), qsTr("%1 - %2").arg(root.active.trackArtist).arg(root.active.trackTitle), "music_note");
             }
         }
 
@@ -62,7 +62,7 @@ Singleton {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "mediaToggle"
-        description: "Toggle media playback"
+        description: "切换媒体播放状态"
         onPressed: {
             const active = root.active;
             if (active && active.canTogglePlaying)
@@ -74,7 +74,7 @@ Singleton {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "mediaPrev"
-        description: "Previous track"
+        description: "上一首"
         onPressed: {
             const active = root.active;
             if (active && active.canGoPrevious)
@@ -86,7 +86,7 @@ Singleton {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "mediaNext"
-        description: "Next track"
+        description: "下一首"
         onPressed: {
             const active = root.active;
             if (active && active.canGoNext)
@@ -98,7 +98,7 @@ Singleton {
     CustomShortcut {
         // qmllint enable unresolved-type
         name: "mediaStop"
-        description: "Stop media playback"
+        description: "停止媒体播放"
         onPressed: root.active?.stop()
     }
 
diff --git a/services/VPN.qml b/services/VPN.qml
index a1ed0dda9991a6539fd5a2a5f49b166a0a14305e..5d2cb8459db5f6fab994a2c57e09b0cae42cddf6 100644
--- a/services/VPN.qml
+++ b/services/VPN.qml
@@ -216,7 +216,7 @@ Singleton {
             status.reason = "WARP registration required";
         } else if (!output.includes("Disconnected")) {
             status.state = "error";
-            status.reason = "Unknown WARP status";
+            status.reason = "未知 WARP 状态";
         }
         return status;
     }
@@ -286,19 +286,19 @@ Singleton {
 
         switch (statusObj.state) {
         case "connected":
-            Toaster.toast(qsTr("VPN connected"), qsTr("Connected to %1").arg(displayName), "vpn_key");
+            Toaster.toast(qsTr("VPN 已连接"), qsTr("已连接到 %1").arg(displayName), "vpn_key");
             break;
         case "disconnected":
-            Toaster.toast(qsTr("VPN disconnected"), qsTr("Disconnected from %1").arg(displayName), "vpn_key_off");
+            Toaster.toast(qsTr("VPN 已断开"), qsTr("已断开与 %1 的连接").arg(displayName), "vpn_key_off");
             break;
         case "needs-auth":
             const authMsg = statusObj.reason || "Authentication required";
-            Toaster.toast(qsTr("VPN authentication required"), qsTr("%1: %2").arg(displayName).arg(authMsg), "vpn_lock");
+            Toaster.toast(qsTr("VPN 需要认证"), qsTr("%1: %2").arg(displayName).arg(authMsg), "vpn_lock");
             break;
         case "error":
             if (status.state === "connected" || status.state === "connecting" || status.state === "needs-auth") {
-                const errMsg = statusObj.reason || "Unknown error";
-                Toaster.toast(qsTr("VPN error"), qsTr("%1: %2").arg(displayName).arg(errMsg), "error");
+                const errMsg = statusObj.reason || "未知错误";
+                Toaster.toast(qsTr("VPN 错误"), qsTr("%1: %2").arg(displayName).arg(errMsg), "error");
             }
             break;
         }
diff --git a/services/Weather.qml b/services/Weather.qml
index 7c12569281c51904db706f7f2a7c658bfd227926..6311b76046e879df3cc8659be4c3c10282b21cb4 100644
--- a/services/Weather.qml
+++ b/services/Weather.qml
@@ -27,7 +27,7 @@ Singleton {
     }
 
     readonly property string icon: cc ? Icons.getWeatherIcon(cc.weatherCode) : "cloud_alert"
-    readonly property string description: cc?.weatherDesc ?? qsTr("No weather")
+    readonly property string description: cc?.weatherDesc ?? qsTr("暂无天气信息")
     readonly property string temp: formatTemp(cc?.tempC)
     readonly property string feelsLike: formatTemp(cc?.feelsLikeC)
     readonly property int humidity: cc?.humidity ?? 0
@@ -130,7 +130,7 @@ Singleton {
                     city = fixCityName(geoCity);
                     cachedCities.set(coords, city);
                 } else {
-                    city = "Unknown City";
+                    city = "未知城市";
                 }
             });
         };
@@ -247,36 +247,36 @@ Singleton {
 
     function getWeatherCondition(code: string): string {
         const conditions = {
-            "0": "Clear",
-            "1": "Clear",
-            "2": "Partly cloudy",
-            "3": "Overcast",
-            "45": "Fog",
-            "48": "Fog",
-            "51": "Drizzle",
-            "53": "Drizzle",
-            "55": "Drizzle",
-            "56": "Freezing drizzle",
-            "57": "Freezing drizzle",
-            "61": "Light rain",
-            "63": "Rain",
-            "65": "Heavy rain",
-            "66": "Light rain",
-            "67": "Heavy rain",
-            "71": "Light snow",
-            "73": "Snow",
-            "75": "Heavy snow",
-            "77": "Snow",
-            "80": "Light rain",
-            "81": "Rain",
-            "82": "Heavy rain",
-            "85": "Light snow showers",
-            "86": "Heavy snow showers",
-            "95": "Thunderstorm",
-            "96": "Thunderstorm with hail",
-            "99": "Thunderstorm with hail"
+            "0": "晴",
+            "1": "晴",
+            "2": "局部多云",
+            "3": "阴",
+            "45": "雾",
+            "48": "雾",
+            "51": "毛毛雨",
+            "53": "毛毛雨",
+            "55": "毛毛雨",
+            "56": "冻毛毛雨",
+            "57": "冻毛毛雨",
+            "61": "小雨",
+            "63": "中雨",
+            "65": "大雨",
+            "66": "小冻雨",
+            "67": "大冻雨",
+            "71": "小雪",
+            "73": "中雪",
+            "75": "大雪",
+            "77": "雪粒",
+            "80": "小阵雨",
+            "81": "阵雨",
+            "82": "强阵雨",
+            "85": "小阵雪",
+            "86": "大阵雪",
+            "95": "雷暴",
+            "96": "雷暴伴冰雹",
+            "99": "强雷暴伴冰雹"
         };
-        return conditions[code] || "Unknown";
+        return conditions[code] || "未知";
     }
 
     onLocChanged: fetchWeatherData()
